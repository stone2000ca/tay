# Run #007 — 2026-05-17

**Duration:** 18:26 → 18:50 UTC (~24 min)
**Milestone:** v0.7 — Gmail OAuth + send path (review queue, send, audit, trust events)
**Branch / worktree:** `worktree-agent-ac7bca9de2fc0e3c7` (deleted post-merge)

## Goal

First milestone where Tay emits. All 7 Tay gates active. OAuth-token handling adds its own risk surface (encryption at rest, no token logging).

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v0.7 Gmail OAuth + send | `agent-ac7bca9de2fc0e3c7` | COMPLETE |
| Quality judge | (read worktree diffs) | APPROVED |

## What landed

- Migrations 0006 (google_oauth) + 0007 (sent_messages + trust_events)
- OAuth: AES-256-GCM crypto, raw-fetch google client, encrypt-at-rest persist with `ensureFreshAccessToken`
- Routes: `/api/auth/google/{start,callback}` with CSRF state cookie (randomBytes(32), 10-min TTL)
- Send pipeline: `lib/suppression/check.ts` (stub), `lib/trust/record.ts` (best-effort), `lib/send/gmail.ts` (RFC 5322 + base64url + RFC 2047), `lib/send/orchestrate.ts` (chokepoint with explicit ordering)
- UI: `/queue` server component + per-row send actions; `/settings` rewritten with status dots + connect/disconnect
- 216/216 tests across 21 files (added 30+)

## Tay-specific gates (ALL ACTIVE for first time)

- B: ✅ — schemas operational only
- C: ✅ — upstream (judge enforces; orchestrator only sends judge=allow drafts)
- D: ✅ — upstream (drafter enforces rubric)
- E: ✅ — `isSuppressed` called BEFORE `ensureFreshAccessToken` (test locks ordering)
- F: ✅ — `appendAudit({ action: "send.sent" })` on success; also oauth.{connected,disconnected}
- H: ✅ — no new untrusted-input surface; CSRF on OAuth state; code only forwarded to Google
- I: ✅ — `recordTrustEvent` on every Tier-3 outcome (sent / blocked_by_judge / blocked_by_suppression)

## Read-vs-write contract for new modules

- `lib/oauth/crypto.ts` — pure; throws on missing/malformed/tampered
- `lib/oauth/google.ts` — WRITE-style network; throws on non-200; HTTP status only on error (no body)
- `lib/oauth/persist.ts` — save/delete throw (write); get soft-fails to null (read); ensureFreshAccessToken hybrid (throws — orchestrator catches)
- `lib/suppression/check.ts` — stub (false); v0.8 must default to TRUE on read error (header notes)
- `lib/trust/record.ts` — best-effort; never throws
- `lib/send/gmail.ts` — discriminated `SendResult`; never throws to caller
- `lib/send/orchestrate.ts` — chokepoint; discriminated; each precondition fast-fails

## Judge verdict

APPROVED. Process 5/5, Product 4/5.

Highlights from the judge:
- "This is the most carefully-engineered PR in the build. Every load-bearing surface has a comment explaining intent + threat model."
- "The log-probe test for recipient/subject/body/token non-leakage is the single highest-leverage test in the codebase."
- "Gate ordering has an explicit unit test, not 'verified by reading the code'."

Two non-blocking flags for v0.8:
1. `sent_messages.draft_id` has no UNIQUE constraint. Orchestrator's "already sent?" guard is racy under concurrent clicks. Single-tenant attenuates but doesn't eliminate (two browser tabs).
2. `sendDraftAction` silent-failure path: returns void + console.warn + revalidatePath; user gets no flash banner.

Five other non-blocking observations:
- `saveGoogleOAuth` DELETE+INSERT not transactional (edge-case disconnect mid-flow)
- Subject not in audit redactor matcher list (consistent with "callers must pre-redact" but worth a v0.8 pass)
- Suppression stub returns false — v0.8 MUST implement "default to TRUE on read error" semantics
- Per-render env reads on /queue could be cached
- `audit_log` chain integrity becomes more critical now that send events are written; verifier endpoint covers it

## Rollback

- Code: `git revert d839b071`
- DB:
```sql
DROP INDEX IF EXISTS trust_events_capability_event_type_idx;
DROP TABLE IF EXISTS trust_events;
DROP INDEX IF EXISTS sent_messages_prospect_id_sent_at_idx;
DROP TABLE IF EXISTS sent_messages;
DROP TABLE IF EXISTS google_oauth;
```

## Commit / PR / Merge

`9ac7642` → squashed as `d839b071`. [PR #13](https://github.com/stone2000ca/tay/pull/13).

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score | 5/5 |
| Product score | 4/5 |
| Tests | 216/216 (added 30+) |
| Run time | ~24 min |

## State after

**Roadmap progress:** 8/10 milestones MERGED.

**Next up:** v0.8 — Suppression list + unsubscribe handling. Real `isSuppressed()` implementation; `suppression` table; unsubscribe webhook endpoint; address v0.7 carry-forwards (UNIQUE on sent_messages.draft_id; sendDraftAction error flash).

## Process improvements

(From judge's top 2)
1. Whenever the orchestrator has a "has this happened?" read-then-write guard, the DB schema MUST carry a UNIQUE constraint enforcing the same invariant. Add to judge-prompt.md checklist.
2. Server actions triggered from form buttons MUST surface errors via redirect-with-?error. `console.warn + revalidatePath` leaves the user uncertain.

## Escalations

None blocking. Two items the user should know:
1. Double-send race (no UNIQUE on draft_id) — fix in v0.8 before any production traffic
2. `/queue` does live env checks every render — v1.0 candidate to cache

---

*Auto-generated by /tay-build · Run #007 · 2026-05-17*
