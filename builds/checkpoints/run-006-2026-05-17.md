# Run #006 — 2026-05-17

**Duration:** 18:05 → 18:25 UTC (~20 min)
**Milestone:** v0.6 — Audit log v1 (sha256 hash chain over Tier-3 events) + v0.5 carry-forwards
**Branch / worktree:** `worktree-agent-a4767ec19e27a8bbe` (deleted post-merge)

## Goal

Replace `appendAudit` stub with a real tamper-evident hash chain. Wire `/api/audit/verify` + `/audit` UI. Address the 3 v0.5 carry-forwards (redactor doc/matcher, neuter opener belt-and-braces, sanitizeReasons design comment).

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v0.6 audit log + verifier + carry-forwards | `agent-a4767ec19e27a8bbe` | COMPLETE |
| Quality judge | (read worktree diffs) | APPROVED |

## What landed

- `lib/audit/hash.ts` — pure `computeHash` + `canonicalJson` (recursive key-sort) + `NULL_PREV_HASH_SENTINEL`
- `lib/audit/append.ts` rewritten (public API preserved) — reads latest `this_hash`, redacts before hashing, never throws
- `lib/audit/verify.ts` — discriminated `{ ok, totalRows, lastHash | brokenAt }` walker
- `GET /api/audit/verify` + `/audit` page (server component; nav link added)
- Migration `0005_audit_index.sql` — index on `(occurred_at DESC, id DESC)`; new `index` sentinel kind in migrate.ts
- v0.5 carry-forwards all addressed (redactor expanded + per-key parameterized test; opener neuter in both prompt files; sanitizeReasons doc-comment)
- 143/143 tests (added 37 + 2 opener-neuter)

## Tay-specific gates

- B: ✅ — audit_log operational only; expanded redactor catches email/body/raw/raw_body/prospect_email plus the secret-shaped keys
- F: ✅ **FULLY LIVE** — judge.decision events from v0.5 now write tamper-evident rows; `/api/audit/verify` exposes integrity check
- H: ✅ — neuter opener belt-and-braces; canonical JSON prevents key-reorder hash manipulation
- C, D, E, I: N/A

## Judge verdict

APPROVED. Process 5/5, Product 4/5.

Two non-blocking v0.7+ polishes:
1. Hash concatenation has no explicit domain separator (currently safe-by-coincidence). v0.7 polish: insert `\x1f` separators.
2. `AuditVerifyResult` lumps `supabase_unavailable` / `read_error` under `brokenAt` with sentinel zeros; cleaner shape would be a top-level discriminated `{ok:false, kind:"chain_broken"|"unavailable"}`. UI handles correctly today.

## Hash determinism guarantee

- No `Date.now()` in hash path — caller supplies `occurred_at`
- No `Math.random()` anywhere
- `canonicalJson` recursively sorts object keys → same payload with reordered keys → identical hash (tested)
- `NULL_PREV_HASH_SENTINEL = "NULL"` for first row → well-defined and reproducible
- Verifier and writer import the SAME `computeHash` — bit-for-bit symmetry

## Concurrency caveat

Two parallel `appendAudit` calls could read the same `prev_hash` and break the chain. Documented in the file header. Acceptable for single-tenant low-concurrency v0.6; v1.0 candidate to add row-level lock or serializable transaction. Verifier surfaces the break as `prev_hash_mismatch`.

## Rollback

- Code: `git revert 39f5c93d`
- DB: no DROP needed (audit_log existed since v0.2; `0005_audit_index` safe to leave)
- Optional: `TRUNCATE audit_log;` for a fresh chain on re-ship

## Commit / PR / Merge

`936d509` → squashed as `39f5c93d`. [PR #11](https://github.com/stone2000ca/tay/pull/11).

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score | 5/5 |
| Product score | 4/5 |
| Tests | 143/143 (was 96; added 39) |
| Run time | ~20 min |

## State after

**Roadmap progress:** 7/10 milestones MERGED (was 6/10).

**Next up:** v0.7 — Gmail OAuth + send path. The first milestone where Tay actually emits to a third party. Tay gates B/C/D/F/H ALL active; E (suppression) becomes load-bearing (must check before send — but v0.8 is where the list itself lands; v0.7 calls a stub `isSuppressed()` that always returns false); I (trust-tier writes — `recordTrustEvent`) first appears.

## Process improvements

1. Add a unit test for hash domain separation (today's safe-by-coincidence behavior) so a future refactor doesn't silently introduce a concatenation collision.
2. v0.7 will inherit the audit chain — every send/reply-send call site must call `appendAudit({ action: "send.sent", payload: {...} })`. Plan that into v0.7's manifest.

## Escalations

None new.

---

*Auto-generated by /tay-build · Run #006 · 2026-05-17*
