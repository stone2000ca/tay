# Run #005 — 2026-05-17

**Duration:** 17:43 → 18:02 UTC (~19 min)
**Milestone:** v0.5 (Judge v1 — 4-way decision over drafts + audit-log stub + v0.3/v0.4 paper cuts)
**Branch / worktree:** `worktree-agent-a99ce45a450225e51` (deleted post-merge)
**Trigger:** /tay-build (continuing autonomously)

## Goal

The load-bearing milestone. Every draft passes through a strict 4-way judge (allow/block/revise/escalate) BEFORE display. Wire `appendAudit` stub now (v0.6 wires real hash chain without touching call sites). Clear paper cuts from v0.3 and v0.4 judges.

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v0.5 judge + audit stub + paper cuts | `agent-a99ce45a450225e51` | COMPLETE |
| Quality judge | (read worktree diffs) | APPROVED |

## What landed

**Files added:** `lib/supabase/migrations/0004_judge_decisions.sql`, `lib/judge/{decision-schema,prompt,judge,persist}.ts` + tests, `lib/audit/append.ts`, `lib/voice/constants.ts`, `components/supabase-warning.tsx`, `app/draft/draft-form.tsx`, `app/setup/voice/voice-form.tsx`

**Files modified:** `lib/supabase/migrate.ts` (0004 sentinel), `lib/voice/calibrate.ts` (constants import), `lib/draft/prompt.ts` (closing-tag neuter on all wrapped fields), `lib/draft/prompt.test.ts` (neuter test), `app/draft/actions.ts` (pre-flight + judge wiring), `app/draft/page.tsx` (server-wrapper split), `app/setup/voice/page.tsx` (same pattern + warning banner), `README.md`

**Tests:** typecheck PASS, build PASS (7 routes), 96/96 vitest (added 47)

## Tay-specific gates

- B: ✅ judge enumerates protected categories as block triggers; no special-cat columns in `judge_decisions`; judge instructed NOT to emit category names in its own reasons (defense-in-depth at the seam)
- C: ✅ canonical disclosure marker named in judge prompt; missing → revise with corrected body
- D: ✅ full rubric verbatim in judge system prompt; every field tested for presence
- F (partial): ✅ `appendAudit` stub wired on every judge decision; payload pre-redacted (draftId + decision string + modelUsed). v0.6 swaps real hash chain.
- H: ✅ untrusted_source wrap + closing-tag neuter (tested) + response_format json_object + stripJsonFences + `parseJudgeDecision` hard validator + SDK error suppression
- E, I: N/A

## Read-vs-write contract

- `judgeDraft` (LLM): never throws, returns discriminated union
- `saveJudgeDecision` (WRITE): throws on DB error; action layer catches and degrades
- `getLatestDecisionForDraft` (READ): soft-fails to null
- `appendAudit`: NEVER throws (audit is best-effort)

## Wizard degraded-state matrix for /draft

- app_config missing → redirect /setup
- voice rubric missing → red banner; judge never reached
- Supabase env missing → SupabaseWarning banner + pre-flight returns friendly error (no LLM call burned)
- OPENROUTER_API_KEY missing → mapped to "Could not reach the drafter right now"
- judge LLM fails → degraded-mode visibility: draft saved + judge "unavailable" banner; v0.7 send path will block
- judge returns malformed → same degraded path

## Judge verdict

APPROVED. Process 5/5, Product 4/5. Three non-blocking flags for v0.6:
1. `appendAudit` redactor doc-vs-impl drift — comment claims it catches email/body, but matcher only catches api_key/secret/token/password. No active leak (v0.5 callers don't pass email) but v0.7 send-event callers must not trust the comment.
2. `neuter()` covers `</untrusted_source>` only; defense-in-depth would also sanitize the opener (one-line cheap).
3. `sanitizeReasons` truncates entries >500 chars rather than rejecting. Intentional (cap-and-keep yields usable decision) but worth documenting.

## Rollback

- Code: `git revert d0aab4d1b6f2106c52d50451699844bbdede2321`
- DB: `DROP TABLE IF EXISTS judge_decisions;`

## Commit / PR / Merge

`e5c404cc` → squashed as `d0aab4d1`. [PR #9](https://github.com/stone2000ca/tay/pull/9). Merged via `gh pr merge --squash --delete-branch --repo`.

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score | 5/5 |
| Product score | 4/5 |
| Run time | ~19 min |

## State after

**Roadmap progress:** 6/10 milestones MERGED (was 5/10).

**Next up:** v0.6 — Audit log v1 (hash chain over `audit_log`). Replace `appendAudit` stub with the real implementation: read prev_hash from latest row, compute `this_hash = sha256(prev_hash + json(payload) + timestamp)`, insert atomically. Audit-trail verifier `/api/audit/verify` returns the chain's integrity status. Address the 3 v0.5 flags (redactor doc, neuter belt-and-braces, sanitizeReasons documentation).

## Process improvements for next run (judge's top 2)

1. When writing a "defensive" comment listing what a function protects, include a unit test that asserts each named protection — a test on `{email: "x@y.com"}` being redacted would have caught the v0.5 drift.
2. When the review brief calls for defense-in-depth and you intentionally implement only half, document the rationale in the code so the next reviewer doesn't re-flag it.

## Escalations

None new.

---

*Auto-generated by /tay-build · Run #005 · 2026-05-17*
