# Run #013 — 2026-05-17 — v1.1.2.5 (IMAP reply polling for SMTP mode)

**Duration:** 23:29 → 23:48 UTC (~19 min)
**Milestone:** v1.1.2.5 — IMAP reply polling for SMTP mode + remove interim banner
**Branch / worktree:** `worktree-agent-a9a21f382c16c6c3e` (deleted post-merge)
**PR:** [#26](https://github.com/stone2000ca/tay/pull/26) — squashed as `e3b8168b`

## Goal

Close the v1.1.2 banner-lie. SMTP-mode users get full reply visibility through the same classify → trust → auto-draft pipeline as OAuth users.

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v1.1.2.5 implementation | `agent-a9a21f382c16c6c3e` | COMPLETE (committed before report) |
| Quality judge | (read worktree diffs) | APPROVED first pass |

## What landed

- Migration 0013: `imap_poll_cursor` (single-row, `lock_col UNIQUE`)
- `lib/reply/imap-poll.ts` via `imapflow@^1.3.3` — UID delta fetching, first-poll seeds cursor with no backfill, never throws, MAX_MESSAGES_PER_POLL cap, logger disabled
- `lib/reply/poll.ts:pollReplies()` channel-aware dispatcher (oauth → existing `pollGmail`; app_password → new `pollImapMailbox`)
- `lib/reply/handle.ts` dual-anchor thread match: existing `gmail_thread_id` lookup unchanged for OAuth; new `gmail_message_id` fallback fires when first match misses AND `inReplyToMessageId` provided (IMAP-only)
- Cron route `/api/cron/poll-gmail` calls channel-aware `pollReplies()` — route name preserved (no vercel.json changes)
- AuditAction.reply.received payload now includes `channel: "oauth" | "app_password"`
- Interim banners removed from `/queue` + `/replies`
- 504 unit tests (added 26: 19 imap-poll + 3 handle dual-anchor + 4 dispatcher) + 10/10 JOURNEYS

## Tay gates

- **H — load-bearing:** IMAP-fetched body flows ONLY through `handleReply` → `classifyReply` → `<untrusted_source>` wrap. No parallel classifier introduced. Grep-verified.
- E: existing `unsubscribe_request → addSuppression` path unchanged
- F: every reply receives audit row tagged with `channel`
- B, C, D, I: unchanged

## Judge verdict

**APPROVED first pass. 5/5 process, 5/5 product.** First clean-on-first-try v1.x milestone.

Two clarifications noted for context:
1. Dual-anchor precondition is "lookup-miss" not "empty-string check" — matches by lookup result, not input shape. Looser than the brief but functionally correct.
2. `imap-poll.ts` derives `port`/`secure` from credentials (not hardcoded 993) — accepts whatever port the v1.1.2 mailbox_credentials schema stores.

## Rollback

- Code: `git revert e3b8168b`
- DB: `DROP TABLE IF EXISTS imap_poll_cursor;`

## Outcomes

| Metric | Value |
|---|---|
| Merged? | yes |
| Process | 5/5 |
| Product | 5/5 |
| Tests | 504/504 unit + 10 journeys |
| Run time | ~19 min |
| Fix-pass rounds | 0 |

---

*Auto-generated by /tay-build · Run #013 · 2026-05-17*
