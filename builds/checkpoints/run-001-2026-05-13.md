# Run #001 — 2026-05-13

**Duration:** 15:59 → 16:19 UTC (~20 minutes)
**Milestone:** v0.1 (setup wizard step 1 — paste Anthropic key, name your instance)
**Branch / worktree:** `worktree-agent-a93c8f7c4d3bbc4ac` (deleted post-merge); merged into `main`
**Trigger:** /tay-build (no args; first real run after bootstrap)

## Goal

Spawn one general-purpose agent for v0.1. The wizard renders at `/setup` with a form for two inputs: Anthropic API key + instance display name. Server action validates the pasted key by calling `claude-haiku-4-5-20251001` via `@anthropic-ai/sdk` with a one-token hello-world prompt. On success, persists app name + validation timestamp via a thin `lib/app-config.ts` abstraction (cookie-backed at v0.1; the abstraction is what v0.2 swaps to Supabase). Home page gains a first-run detector that redirects to `/setup` when the setup cookie is absent. No Supabase yet — v0.2 introduces it. Judge reviews against the general 7 criteria; Tay-specific gate B (no special-category data) applies; gates C/D/E/F/H/I are N/A or light at this milestone.

## Agents spawned

| Task | Subagent type | Worktree | Returned |
|---|---|---|---|
| v0.1 — setup wizard step 1 | general-purpose | `agent-a93c8f7c4d3bbc4ac` | COMPLETE |
| Quality judge | general-purpose | (read diffs from agent worktree) | APPROVED |

## Milestone outcome

### v0.1 — setup wizard step 1

**Plan row:** PLAN.md roadmap, v0.1.

**Status:** MERGED

**Files added:**
- `app/setup/page.tsx`
- `app/setup/actions.ts`
- `lib/anthropic.ts`
- `lib/app-config.ts`
- `lib/app-config.test.ts`

**Files modified:**
- `app/page.tsx` (first-run redirect + cfg.name display)
- `README.md` (one-line v0.1 status note)
- `package.json` (added `@anthropic-ai/sdk@^0.95.2`, `vitest@^4.1.6`, `test` script)
- `package-lock.json` (regenerated)

**Acceptance criteria:**
- ✅ `/setup` wizard renders form (key + name) + explainer + submit
- ✅ Server action validates inputs, calls Anthropic, calls setAppConfig on success
- ✅ Success state shows configured name + env-var instructions
- ✅ Inline error states (invalid-key / rate-limited / network-error / unknown)
- ✅ Error mapping does NOT leak raw SDK errors (could expose account details)
- ✅ `app/page.tsx` redirects to `/setup` when cookie absent; shows cfg.name when present
- ✅ Cookie flags: httpOnly, secure, sameSite=lax, path=/, maxAge=1y
- ✅ Display name regex: ASCII-printable, ≤60 chars (Tay gate B)
- ✅ API key never persisted anywhere — only sent to Anthropic, then GC'd

**Tests:**
- `npm run typecheck`: ✅
- `npm run build`: ✅ (Next 16.2.6 Turbopack — `/setup` static, `/` dynamic per cookie redirect, `/api/health` dynamic, all 5 routes generated)
- `npm run lint`: ⚠️ pre-existing breakage (Next 16 removed `next lint` — see escalations)
- `npm test`: ✅ (4/4 vitest — round-trip, absent cookie, malformed cookie, clear)
- Live Anthropic smoke test: ❌ DEFERRED — no `ANTHROPIC_API_KEY` in orchestrator env. Static evidence only (SDK error classes verified to exist in `@anthropic-ai/sdk@0.95.2`). User will exercise the validator path themselves the first time they run the wizard.

**Tay-specific gates:**
- B (no special-category data): ✅ — wizard collects only API key + name; ASCII regex on name; explicit comment in `actions.ts:9-10` calls out gate B
- C (AI disclosure footer): N/A
- D (voice rubric enforcement): N/A
- E (suppression on send): N/A
- F (audit log on Tier-3): N/A
- H (adversarial-input defenses): ✅ light — pasted key flows only to Anthropic SDK; no `dangerouslySetInnerHTML`; `cfg.name` rendered through React JSX (auto-escaped)
- I (trust-tier writes): N/A

**Write-scope check:**
- Files actually edited: matches manifest in `run-001-IN_PROGRESS.md` (now removed) — see Files added/modified above
- Out-of-scope edits: none

**Rollback plan:**
```bash
git revert 1f87cf1e6a9c081cdb4ec0740425824d92a22778
# Manual:
git checkout HEAD~1 -- README.md app/page.tsx package.json package-lock.json
rm -rf app/setup lib/
npm install
```

**Deviations from scope:**
- Test runner: chose `vitest` over `node --test`. Reason: Node ESM resolver couldn't find `next/headers` for `--experimental-test-module-mocks` (Next doesn't expose subpaths via `package.json` exports outside the bundler). Spec allowed either path.
- `@anthropic-ai/sdk` pinned to `^0.95.2` (latest stable on npm at the time of the run).
- Agent did NOT commit its work before declaring done; orchestrator committed in Phase 5 using the agent's suggested message. (See process improvements.)

**Commit:** `1f87cf1e6a9c081cdb4ec0740425824d92a22778` "feat(setup): wizard step 1 — Anthropic key validation + instance name"
**PR:** https://github.com/stone2000ca/tay/pull/1
**Merge verified via `gh pr view`:** MERGED at 2026-05-13T16:18:22Z
**Vercel preview:** not captured (Vercel deploy not yet wired to the GitHub repo at the time of this run; future runs should capture once linked)

## Quality check (judge verdict)

═══════════════════════════════════════════
MILESTONE VERDICT

v0.1 (setup wizard step 1):
  Status: APPROVED
  Test evidence: `lib/app-config.test.ts` 4/4 vitest pass (round-trip, absent-cookie, malformed-cookie, clear). `npm run typecheck` clean. `npm run build` clean (Next 16.2.6, 5 routes generated including `/setup`). Named SDK error classes (`AuthenticationError`, `RateLimitError`, `APIConnectionError`) verified to exist in `@anthropic-ai/sdk@0.95.2`.
  Tay-specific gates passed: B (form collects only `apiKey` + `name`; ASCII-only regex; explicit comment in `actions.ts`). H-light (apiKey only flows to `Anthropic({ apiKey })`; no `dangerouslySetInnerHTML`; `cfg.name` rendered through JSX text node — auto-escaped).
  Rollback verified: weak — agent's report did not include rollback commands in the supplied summary. Orchestrator filled in the gap; future agents should include in their report.
  Notes: Solid, scoped, non-leaky implementation. Cookie flags correct. API key never persisted. Below-the-line nit: `secure: true` will block the cookie write on plain-HTTP localhost dev — Next dev server defaults to HTTP, so a developer running `npm run dev` may bounce-loop on `/setup`. Worth a one-line note in run-002, not a blocker (Vercel deploys are HTTPS).

═══════════════════════════════════════════
PROCESS SCORE: 4/5

Agent followed scope tightly, self-tested all three gates, flagged real deviations honestly, and respected the write-scope manifest perfectly — but left work uncommitted on the worktree branch despite the report claiming a commit message was prepared, and skipped writing tests for `validateAndSaveSetup` validators and `validateApiKey` error mapping (the seam between Tay and Anthropic).

═══════════════════════════════════════════
PRODUCT SCORE: 4/5

Cleanly scoped, security-correct, idiomatic Next 16 App Router code with thoughtful comments documenting v0.1→v0.2 swap intent; minor gaps are missing test coverage on the SDK error-mapping seam and the `secure: true` cookie flag's localhost-dev failure mode (deploy works, dev loops).

═══════════════════════════════════════════
TOP IMPROVEMENTS FOR NEXT RUN

1. Require the agent to actually `git commit` its work in its worktree before declaring COMPLETE. Add "git status must be clean (no modified, no untracked)" to the self-test gate alongside typecheck/build/test.
2. For any milestone introducing a new external-SDK seam, require at least one test that exercises the error-mapping branches via mocked SDK error throws.

═══════════════════════════════════════════
ESCALATIONS REQUIRING USER INPUT

- `next lint` script in `package.json` is broken (Next 16 removed it). User must decide before v0.2 whether to (a) replace with direct `eslint .` invocation + add ESLint, (b) migrate to Biome, or (c) drop the lint script. Affects every future run's "self-test" definition.
- Cookie `secure: true` will silently fail on plain-HTTP localhost dev, causing infinite redirect to `/setup`. Either accept (dev users tunnel HTTPS or use deployed previews) or relax to `secure: process.env.NODE_ENV === "production"`.

═══════════════════════════════════════════

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score (judge) | 4/5 |
| Product score (judge) | 4/5 |
| Total token spend (estimate) | not tracked this run |
| Total run time | ~20 min |

## State after this run

**Roadmap progress:** 2/10 milestones MERGED (was 1/10 before this run — v0.0.1 scaffold + v0.1 wizard step 1)

**Next up:**
- v0.2 (Supabase Marketplace integration + auto-migrations + UI shell with nav) — eligible

**New blockers:**
- None for v0.2 itself, but two open user decisions (see escalations) that should land before v0.2 starts.

**State machine reconciliation:**
- IN_FLIGHT count at end of run: 0 ✅
- INTERRUPTED count: 0 ✅
- PREVIEW_FAILED count: 0 ✅
- BLOCKED count: 0 ✅
- Agent worktree `agent-a93c8f7c4d3bbc4ac` still on disk (branch deleted from remote); safe to reclaim with `git worktree remove --force` since merge is verified.

## Process improvements for next run

1. **Make agent commit before declaring done.** Bake it into the agent prompt: "git status must be clean (no modified, no untracked) before STATUS=COMPLETE." This avoids the orchestrator-as-committer pattern.
2. **Test the SDK seam.** When a milestone introduces a new external-SDK boundary (Anthropic, Supabase, Gmail, etc.), require the agent to write at least one test per error-class branch using a mock that throws each named SDK error. Saves silent-regression risk on SDK upgrades.
3. **Wire Vercel preview detection.** Once the GitHub repo is linked to a Vercel project, capture the preview URL in the PR comment + checkpoint. Adds a verifiable smoke-test surface beyond local build.
4. **Capture token spend per run.** Anthropic billing dashboard or cumulative agent usage estimates would help spot drift in run cost over the v0.x cycle.

## Escalations to user

1. **Pick a lint story.** `next lint` is dead in Next 16. Decide between direct `eslint .` (add as devDep), Biome, or drop the lint check. Affects the self-test gate from v0.2 onward.
2. **Decide cookie `secure` behavior.** Currently `secure: true` always — works on Vercel HTTPS, breaks plain-HTTP localhost dev (`npm run dev` will infinite-redirect to `/setup`). Suggested fix: `secure: process.env.NODE_ENV === "production"`. One-line change; trivial follow-up but worth a deliberate decision.
3. **No `ANTHROPIC_API_KEY` in orchestrator env.** Future LLM-touching milestones (v0.3 voice calibration, v0.4 drafter, v0.5 judge) WILL need a real key for live smoke tests. Add to `.env.local` (see `.env.example`) before run #003 or accept that those milestones ship with mocked-only verification.

## Notes / surprises

- `gh pr merge --delete-branch` failed at the local-checkout step because `main` is checked out in a parent worktree. The merge itself succeeded server-side. Orchestrator manually deleted the remote branch via `git push origin --delete`. Future runs may want to invoke `gh pr merge` with explicit "no local checkout" flags or skip the local checkout entirely.
- Build emitted a Turbopack workspace-root warning because there's a parent `package-lock.json` at `C:\Users\stone\package-lock.json`. Cosmetic; could be silenced with `turbopack.root` in `next.config.ts` later.

---

*Auto-generated by /tay-build · Run #001 · 2026-05-13*
