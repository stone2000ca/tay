# Run #014 — 2026-05-17 — v1.1.3 (wizard polish: rubric preview + 4 voice paths + test-send + prospect quick-add)

**Duration:** 23:50 → 00:19 UTC (~29 min)
**Milestone:** v1.1.3 — bundles P4 + P5 + P6 from simplification-plan
**Branch / worktree:** `worktree-agent-afb4ef61fb31d8f6b` (deleted post-merge)
**PR:** [#27](https://github.com/stone2000ca/tay/pull/27) — squashed as `2a300f10`

## Goal

Largest single v1.1 milestone. Wizard becomes 10-min end-to-end for non-tech users with the "aha moment" baked into the flow (sample draft → judge approval → test send → first prospect).

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v1.1.3 implementation | `agent-afb4ef61fb31d8f6b` | COMPLETE (committed before report) |
| Quality judge | (read worktree diffs) | APPROVED first pass |

## What landed

**Voice calibration (4 paths):**
- Existing email-paste: minimum sample count relaxed 5 → 1
- New `lib/voice/calibrate-from-description.ts`: anchor email + 3 quick questions
- New `lib/voice/calibrate-from-url.ts`: anchor email + company URL (8s timeout, 1MB cap, content-type allowlist; later v1.1.4 added SSRF allowlist)
- New `lib/voice/calibrate-from-zero.ts`: Path 0 — Tay prompts the user to write a sample on the spot
- All 4 paths require ≥1 real email anchor; all wrap untrusted inputs in `<untrusted_source>` with closing-tag neuter; all produce `parseRubric`-valid rubrics

**Rubric preview & edit:**
- `lib/voice/rubric-display.ts:formatRubricInPlainEnglish` — converts rubric to 4-6 sentence summary
- `/setup/voice/preview` — server-rendered summary + inline editing (formality dropdown, phrase pill editors, tone notes textarea, signature pattern input)

**Sample draft (the "aha moment"):**
- `/setup/voice/sample` — renders draft against canned fake prospect (Alex Chen, VP Sales, Acme Corp) using user's just-calibrated rubric
- Real `generateDraft` + real judge + disclosure footer visible

**Test-send:**
- `/setup/voice/test-send` — full `lib/send/orchestrate.ts` chokepoint (suppression / judge / audit / trust — no bypass); user gets email in own inbox

**Prospect quick-add:**
- `lib/prospect/extract.ts` — cheap-LLM extract from natural-language description
- System prompt forbids demographic inference (gate B defense-in-depth)
- User reviews extracted fields before save
- `/setup/prospect-quickadd` — last wizard step

**Setup-complete state:**
- Migration 0014: `app_config.setup_complete` boolean + `setup_completed_at`
- `markSetupComplete()` flips on prospect save; `getSetupComplete()` soft-fails to false
- `app/page.tsx` redirect chain extended through new sub-flow

**Supporting:**
- `lib/draft/persist.ts:upsertProspect` accepts optional real email (overrides v0.4 .invalid synthesizer; backwards-compatible)
- AuditAction extended: `voice.calibrated`, `setup.completed`
- 535 unit tests (added 33) + 10/10 JOURNEYS

## Tay gates (all 7 active)

- B: prospect-extract + URL-bootstrap system prompts forbid demographic inference
- C: test-send body includes disclosure footer (existing `withDisclosure`); sample-draft preview shows it visibly
- D: rubric is the v0.4 drafter's contract; all 4 paths produce `parseRubric`-valid output; `parseRubric` silently strips hallucinated fields from URL-bootstrap (defense against attacker-controlled content)
- E: test-send goes through real orchestrator → suppression check runs
- F: voice-rubric save writes `voice.calibrated` audit; setup completion writes `setup.completed`
- H: URL content + free-form descriptions + anchor emails all wrapped; URL fetcher never echoes URL in error logs
- I: test-send writes trust event same as production sends

## Judge verdict

**APPROVED first pass. 5/5 process, 5/5 product.** Second consecutive clean-on-first-try milestone.

Two v1.1.4 carry-forwards noted:
1. `/draft?prospectId=…` query param not consumed by existing v0.4 form — prospect saves, redirect lands, but form doesn't pre-fill. UX wart. (Folded into v1.1.4.)
2. URL-fetch path lacks SSRF allowlist (loopback / private-range denylist). Low risk for single-tenant self-hosted but worth `~30-line isPublicHttpUrl()` check. (Folded into v1.1.4.)

## Rollback

- Code: `git revert 2a300f10`
- DB:
```sql
ALTER TABLE app_config DROP COLUMN IF EXISTS setup_completed_at;
ALTER TABLE app_config DROP COLUMN IF EXISTS setup_complete;
```

## Outcomes

| Metric | Value |
|---|---|
| Merged? | yes |
| Process | 5/5 |
| Product | 5/5 |
| Tests | 535/535 unit (+33) + 10 journeys |
| Run time | ~29 min |
| Fix-pass rounds | 0 |

---

*Auto-generated by /tay-build · Run #014 · 2026-05-17*
