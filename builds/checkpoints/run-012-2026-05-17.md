# Run #012 — 2026-05-17 — v1.1.2 (SMTP send / Easy mode)

**Duration:** 22:45 → 23:55 UTC (~70 min including fix-pass)
**Milestone:** v1.1.2 — SMTP send (Easy mode) + channel-aware orchestrator + interim reply-polling banner
**Branch / worktree:** `worktree-agent-ad221122b1c109cfe` (deleted post-merge)

## Goal

Per simplification-plan.md P3a: make installation viable for non-tech users on personal Gmail. SMTP App Password path takes ~2 minutes vs ~20 for Google Cloud OAuth. All 7 Tay gates still apply.

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v1.1.2 implementation | `agent-ad221122b1c109cfe` | COMPLETE (committed before report) |
| Quality judge (first pass) | (read worktree diffs) | NEEDS-FIXES (1 BLOCKER + 4 others) |
| v1.1.2 fix-pass | `agent-ad221122b1c109cfe` (same worktree) | COMPLETE |
| Quality judge (re-judge) | (read fix diffs) | APPROVED |

## What landed

- Migration 0012: `mailbox_credentials` (single-row, `kind CHECK ('oauth','app_password')`)
- `lib/send/smtp.ts` + `smtp-verify.ts` via `nodemailer@^8.0.7` (STARTTLS, RFC-5322 Message-ID, log-probe-tested)
- `lib/mailbox/persist.ts` (unified persistence; backwards-compat fallback to legacy `google_oauth`)
- `lib/send/orchestrate.ts` channel-aware branch (suppression BEFORE branch; both gate-ordering tests pass)
- `/setup/mailbox` wizard step (Easy + Power columns; auth_failed → "switch to Power mode" suggestion)
- Interim banner on `/queue` + `/replies` for SMTP mode (honest about v1.1.2.5 reply polling)
- `/settings` Mailbox section with channel-agnostic copy + Settings-aware disconnect redirect
- v0.7 OAuth callback now dual-writes to BOTH legacy `google_oauth` AND new `mailbox_credentials` (non-fatal mirror)
- AuditAction extended with `mailbox.connected`, `mailbox.disconnected`
- 478 unit tests (added 35) + 10/10 JOURNEYS

## Tay-specific gates

- B: ✅ schema operational only
- C, D: unchanged (upstream of channel branch)
- E: ✅ — suppression check runs BEFORE channel branch; verified by TWO ordering tests (one per channel)
- F: ✅ — `send.sent` audit includes `channel` tag; mailbox.{connected,disconnected} new events
- H: ✅ — SMTP password never logged; password field is `type="password"` + `autocomplete="off"`; STARTTLS errors map to friendly messages without server-response leakage
- I: ✅ — `recordTrustEvent` tagged with channel

## Read-vs-write contract for new modules

- `lib/send/smtp.ts` / `smtp-verify.ts` — WRITE-style (network); returns discriminated union; never throws to caller; logs only one-word error categories
- `lib/mailbox/persist.ts` — WRITE throws (save/clear), READ soft-fails (get/getKind); backwards-compat fallback reads legacy `google_oauth` when new table empty

## Judge process

**First pass: NEEDS-FIXES** (Process 4/5, Product 3/5)

Issues:
1. **BLOCKER:** `/queue` Send button disabled for SMTP-only users — page derived `canSend` from legacy `oauth` table; for SMTP install, `oauth` was always null
2. **MEDIUM:** Same root cause on `/replies` — misleading "Gmail not connected" banner for SMTP users
3. **MEDIUM:** `disconnectMailboxAction` always redirected to wizard, bouncing Settings users
4. **MEDIUM:** `lib/mailbox/persist.ts` header doc was misleading (claimed first-write-to-new-table wins, but Power-mode OAuth callback wrote only to legacy)
5. **LOW:** Settings flash banners used "Gmail" language

**Fix-pass commit `b9a7e98`:** all 5 fixes addressed. Option (b) chosen for Fix 4 (OAuth callback dual-writes to both tables — more honest than just updating the doc).

**Re-judge: APPROVED.** Final Process 4/5, Product 4/5.

The blocker was a real shipped-broken bug — the wizard would have completed successfully but the user would have hit a disabled Send button on first prospect. Half-migration: the agent updated the orchestrator + persistence + wizard but left two consumer pages reading the legacy oauth signal for their send-readiness gates. Judge correctly flagged this as the headline issue; fix landed cleanly.

## Rollback

- Code: `git revert 92049d35`
- DB: `DROP TABLE IF EXISTS mailbox_credentials;` (additive only; legacy `google_oauth` keeps working)

## Commits / PR / Merge

- `79af3df` — feat(v1.1.2): initial implementation
- `b9a7e98` — fix(v1.1.2): wire SMTP through /queue + /replies + disconnect redirect (judge fix-pass)
- Squashed as `92049d35a4313a406db4dac522c7d3cbe7817a33`
- [PR #24](https://github.com/stone2000ca/tay/pull/24)

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score | 4/5 |
| Product score | 4/5 (3 initially; the blocker dropped it; the fix-pass earned it back) |
| Tests | 478 unit (added 35) + 10 journeys |
| Run time | ~70 min including fix-pass |
| Fix-pass rounds | 1 |

## State after

**Roadmap progress:** v1.0 complete (11/11); v1.1.1 + v1.1.2 merged (2/5 v1.1 milestones).

**Next up — v1.1.2.5:** IMAP reply polling for SMTP mode. The interim banner that v1.1.2 added on `/queue` + `/replies` is the contract — v1.1.2.5 removes it once IMAP polling works.

## Process improvements

(From judge's top 2)
1. When introducing a channel-aware abstraction, `grep` for every consumer of the legacy signal it replaces; migrate or document why each stays. Two-minute check would have caught the `/queue` half-migration.
2. For wizard-completing milestones: mentally walk through as a brand-new user end-to-end — finish wizard → primary action surface → click primary action. The blocker was invisible from unit tests but obvious from the user's path.

## Escalations

None blocking.

---

*Auto-generated by /tay-build · Run #012 · 2026-05-17*
