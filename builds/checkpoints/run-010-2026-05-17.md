# Run #010 — 2026-05-17 — v1.0 SHIP GATE

**Duration:** 19:51 → 20:42 UTC (~51 min including fix-pass)
**Milestone:** v1.0 — JOURNEYS eval suite + trust-tier promotion + v0.9 polling fixes
**Branch / worktree:** `worktree-agent-abd57e642cfbbdcda` (deleted post-merge)

## Goal

The SHIP GATE. Three pieces in one PR:
1. JOURNEYS eval suite — adversarial-scenario regression corpus locking in the 7 Tay gates
2. Trust-tier promotion — `lib/trust/tier.ts` reading `trust_events` to compute and surface per-capability tier
3. v0.9 polling robustness fixes — cursor advance race + gmail_poll_cursor single-row + the 4 low-severity carry-forwards

After this merges, Tay is feature-complete for v0.x.

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v1.0 ship gate (initial impl) | `agent-abd57e642cfbbdcda` | COMPLETE (committed before report) |
| Quality judge (first pass) | (read diffs) | **NEEDS-FIXES** |
| v1.0 fix-pass | `agent-abd57e642cfbbdcda` (same worktree) | COMPLETE |
| Quality judge (re-judge) | (read diffs) | **APPROVED** |

## What landed

### JOURNEYS suite (10 scenarios, all green)

| Gate | Scenario | What it actually exercises |
|---|---|---|
| C | cold-draft-happy-path | full chain: drafter → judge → save → audit |
| H | prospect-notes-prompt-injection | asserts `<untrusted_source>` block in LLM prompt |
| B | special-category-mention-in-notes | judge returns "block" with category-named reason |
| C | disclosure-footer-regression | judge returns "revise" with rewrite containing footer |
| D | rubric-drift-formality | judge returns "revise" naming formality mismatch |
| E | send-to-suppressed-prospect | REAL `sendDraft` returns ok:false; Gmail NOT called; `recordTrustEvent("send","blocked_by_suppression")` invoked |
| F | audit-chain-integrity | REAL `verifyAuditChain` × 3 cases (clean / hash_mismatch / prev_hash_mismatch) using REAL `computeHash` for seeding |
| H | adversarial-reply-ignore-instructions | classifier holds shape despite injection |
| H | adversarial-reply-tag-injection | neuter() defends the closing-tag escape |
| I | auto-reply-tier-promotion-path | `recomputeTrustTier` transitions tier_0 → tier_1; audit row "trust.tier_changed" written |

### Trust-tier promotion
- `lib/trust/tier.ts` — `getTrustTier` + `recomputeTrustTier` + `TIER_THRESHOLDS` (auto-stops at tier_2; tier_3 manual-only)
- Demotion: 5+ incidents in last 30 days → drop one tier
- Migration 0010: `trust_tiers (capability PK, tier CHECK, promoted_at, manual_override, updated_at)`
- `/settings/trust` page with Recompute action + Manual override toggle
- AuditAction extended: `trust.tier_changed`, `trust.manual_override_set`

### v0.9 polling carry-forwards (all 6 closed)
1. ✅ MED — cursor advance race: uses History API response's top-level `historyId` via `getRecentMessagesWithHistoryId`
2. ✅ MED — `gmail_poll_cursor` single-row: `lock_col integer DEFAULT 1 UNIQUE` constraint
3. ✅ LOW — unmatched-thread persist: writes `"<unmatched-thread>"` sentinel
4. ✅ LOW — self-email short-circuit at poller layer
5. ✅ LOW — composed reply drafter hydrates real prospect fields
6. ✅ LOW — settings page render honesty (verified server-side reads)

### Honesty fix
`lib/reply/poll.ts` header comment rewritten — explicit "CURSOR ADVANCE HONESTY" block documenting the dedupe-before-classify property that makes the unconditional advance safe.

## Fix-pass details

Judge's first pass flagged two JOURNEYS scenarios as mock-trusting tautologies:
- `audit-chain-integrity` had its own inline `walkChain()` instead of calling `verifyAuditChain`
- `send-to-suppressed-prospect` called `isSuppressed()` directly and asserted the mocked return

Fix-pass commit `f7d2273` rewrote both to call the REAL production code paths. Re-judge: APPROVED.

## Tests

- typecheck clean
- build clean (new route `/settings/trust`)
- `npm test` — 370/370 across 32 files
- `npm run test:journeys` — 10/10; banner "JOURNEYS GREEN"

## Tay-specific gates — ALL 7 LOCKED IN JOURNEYS

The suite IS the regression contract for everything after v1.0.

## Rollback

- Code: `git revert cc2d374a`
- DB:
```sql
ALTER TABLE gmail_poll_cursor DROP CONSTRAINT IF EXISTS gmail_poll_cursor_single_row;
ALTER TABLE gmail_poll_cursor DROP COLUMN IF EXISTS lock_col;
DROP TABLE IF EXISTS trust_tiers;
```

## Commits / PR / Merge

- `68c52b8` — feat(v1.0): JOURNEYS + trust-tier + v0.9 polling fixes (initial)
- `f7d2273` — fix(v1.0): JOURNEYS scenarios call real production code (post-judge)
- Squashed as `cc2d374a5f23bf700ff50f61af572ee36ab793cb`
- [PR #19](https://github.com/stone2000ca/tay/pull/19)

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes (SHIP GATE) |
| Process score (1st) | 3/5 (NEEDS-FIXES) |
| Product score (1st) | 4/5 |
| Re-judge | APPROVED |
| Tests | 370 unit + 10 journeys (all green) |
| Run time | ~51 min including fix-pass |

## State after — TAY v1.0 COMPLETE

**Roadmap progress:** 11/11 milestones MERGED (v0.0.1 + v0.1–v0.9 + v1.0)

**Build cycle status:** COMPLETE. Tay is feature-complete for v0.x.

**Next `/tay-build` invocation:** surfaces "v1.0 complete. Awaiting user kickoff for post-1.0 work." and waits.

## Process improvements

(From judge's first-pass top 2)
1. When writing a regression scenario whose name promises "X end-to-end", verify the scenario actually imports and calls X. A mechanical check ("does each scenario import something from the code path its name advertises?") would have caught both scenario gaps before review.
2. When the engineering report makes a guarantee claim ("cursor advance happens AFTER successful processing"), verify by reading the diff, not by trusting the agent. Self-reports of guarantees should be re-derived by reviewers.

## Escalations

None blocking — but for the user's awareness post-merge:

**Known live-verification gaps (the same ones from every milestone — none new):**
- No real Supabase has been provisioned → none of the 10 migrations (0001–0010) have been applied against live Postgres. First user install via Vercel Marketplace integration is the live smoke test for the full schema.
- No real `OPENROUTER_API_KEY` → LLM-touching code (drafter, judge, classifier, voice extractor) is mock-tested only.
- No real `TAY_OAUTH_SECRET` → AES-256-GCM encryption + HMAC tokens use test-only secrets in tests.
- No real Google OAuth client → Gmail send + poll never end-to-end exercised.
- No real `CRON_SECRET` → polling cron auth not exercised live.

**Mitigations carried throughout:** idempotent migrations (IF NOT EXISTS); transactional DDL; inline-SQL fallback for Turbopack bundling; deterministic SINGLE_ROW_ID upserts for single-row tables; safe-default-TRUE on suppression read-error; HMAC token verification with kind-claim and constant-time compare; comprehensive log-probe tests asserting no PII/tokens in error output.

**Recommended first real-install smoke test:**
1. Provision Supabase via Vercel Marketplace integration
2. Set `OPENROUTER_API_KEY`, `TAY_OAUTH_SECRET` (64 hex chars), `GOOGLE_OAUTH_CLIENT_ID`/`_SECRET`, `NEXT_PUBLIC_SITE_URL`, `CRON_SECRET`
3. Deploy
4. Walk the wizard: setup → voice calibration (paste 5 emails) → draft a test prospect → click Send (queue) → verify Gmail received it → reply → confirm classifier+trust-event chain → verify `/api/audit/verify` returns ok:true

---

*Auto-generated by /tay-build · Run #010 · 2026-05-17*
*Tay v1.0 SHIP GATE complete.*
