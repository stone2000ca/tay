# Run #002 — 2026-05-17

**Duration:** 13:19 → 13:38 UTC (~19 minutes)
**Milestone:** v0.2 (Supabase Marketplace integration + auto-migrations + UI shell with nav)
**Branch / worktree:** `worktree-agent-adf8210112bc0135e` (deleted post-merge); merged into `main`
**Trigger:** /tay-build (no args; user authorized autonomous run-to-completion)

## Goal

Spawn one general-purpose agent for v0.2. Tightly-coupled milestone (Supabase clients, auto-migration runner, app-config refactor, UI shell) that benefits from one-agent coherence over parallel splitting. Judge will review against general 7 criteria + Tay gates B (no special-category data — verify schema), F (audit_log shape readiness for v0.6 hash chain), and H-light (XSS on nav). Roll up resolved escalations from run #001 (drop `next lint`, fix cookie `secure` flag for localhost dev). Defer the ANTHROPIC_API_KEY escalation (v0.2 has no LLM calls).

## Agents spawned

| Task | Subagent type | Worktree | Returned |
|---|---|---|---|
| v0.2 — Supabase + migrations + UI shell | general-purpose | `agent-adf8210112bc0135e` | COMPLETE |
| Quality judge | general-purpose | (read diffs from agent worktree) | APPROVED |
| v0.2 — cold-start race fix | general-purpose (same agent worktree) | `agent-adf8210112bc0135e` | COMPLETE |

## Milestone outcome

### v0.2 — Supabase Marketplace integration + auto-migrations + UI shell with nav

**Plan row:** PLAN.md roadmap, v0.2.

**Status:** MERGED

**Files added:**
- `lib/supabase/server.ts`
- `lib/supabase/anon.ts`
- `lib/supabase/migrations/0001_init.sql`
- `lib/supabase/migrate.ts`
- `lib/supabase/migrate.test.ts`
- `components/nav.tsx`
- `app/settings/page.tsx`

**Files modified:**
- `lib/app-config.ts` (cookie-only → dual backend; `secure` flag now NODE_ENV-gated)
- `lib/app-config.test.ts` (added Supabase-backend tests; 4 → 8 tests)
- `app/page.tsx` (calls `ensureSchema()`; version footer bumped)
- `app/layout.tsx` (mounts `<Nav/>`)
- `app/setup/actions.ts` (calls `ensureSchema()` at top of `validateAndSaveSetup` — cold-start fix)
- `package.json` (added `@supabase/supabase-js`, `pg`, `@types/pg`; removed dead `lint` script)
- `package-lock.json` (regenerated)
- `README.md` (Status bumped to v0.2; install step 2 mentions auto-migrations)
- `.env.example` (added `POSTGRES_URL` / `POSTGRES_URL_NON_POOLING` with explanation)

**Acceptance criteria:**
- ✅ Supabase clients lazy + env-checked; never log raw keys
- ✅ `0001_init.sql` ships `app_config` + `prospects` + `audit_log` (with `prev_hash` / `this_hash` ready for v0.6); all CREATEs `IF NOT EXISTS`; pgcrypto extension declared
- ✅ `ensureSchema()` returns `{ ran, skipped, error? }`, never throws, module-Promise cached, info-schema pre-check, transaction-wrapped
- ✅ Inline-SQL fallback for Turbopack-bundling robustness
- ✅ `lib/app-config.ts` keeps public API; Supabase backend single-row DELETE+INSERT; cookie fallback preserved with `secure: process.env.NODE_ENV === "production"` (resolves run #001 dev-loop bug)
- ✅ Nav mounted in root layout; active-link styling via `usePathname`; suppressed on `/setup` (clean first-run UX)
- ✅ `/settings` placeholder page resolves
- ✅ `app/setup/actions.ts` calls `ensureSchema()` before first Supabase write (closes cold-start race the judge flagged)
- ✅ `next lint` script removed; README has no `npm run lint` reference

**Tests:**
- `npm run typecheck`: ✅ clean
- `npm run build`: ✅ clean (Next 16.2.6 Turbopack; 5 routes — `/` dynamic, `/_not-found` + `/settings` + `/setup` static, `/api/health` dynamic); benign Turbopack NFT-trace warning on `migrate.ts` disk-load (documented inline — INIT_SQL_INLINE is runtime source of truth)
- `npm test`: ✅ 11/11 (8 app-config covering both backends + 3 migrate covering skip-path + Promise dedupe + cache reset)
- Live Supabase smoke test: ❌ DEFERRED — no Supabase project linked in build env. Migration code-only verified. First user install via the Marketplace will be the live smoke test.

**Tay-specific gates:**
- B (no special-category data): ✅ — `0001_init.sql` re-read line-by-line; only operational columns (id/name/validated_at/email/full_name/company/action/payload/hash). Tay gate enforced explicitly in the agent prompt.
- C (AI disclosure footer): N/A
- D (voice rubric enforcement): N/A
- E (suppression on send): N/A
- F (audit log on Tier-3): ✅ — table created with `prev_hash` + `this_hash` text columns staked out so v0.6 hash-chain wiring needs no schema migration. Not yet written-to (correct for v0.2).
- H (adversarial-input defenses, light): ✅ — nav renders only literal strings via JSX text nodes; `cfg.name` (only user-supplied value rendered) auto-escaped; no `dangerouslySetInnerHTML` anywhere
- I (trust-tier writes): N/A

**Write-scope check:**
- Files actually edited: match the manifest in `run-002-IN_PROGRESS.md` PLUS the deliberate post-judge expansion to `app/setup/actions.ts` (one-line fix authorized by orchestrator to close cold-start hole)
- Out-of-scope edits: none. No orchestrator-owned files (`builds/`, `PLAN.md`, `skills/`) touched.

**Rollback plan:**
```bash
# Code revert
git revert 1bcf341e975029e782f97b551f0fd1dc3c2ea478

# DB rollback (for any user who already migrated):
DROP TABLE IF EXISTS audit_log;
DROP TABLE IF EXISTS prospects;
DROP TABLE IF EXISTS app_config;
-- DROP EXTENSION pgcrypto;  -- optional; usually safe to leave
```

**Deviations from scope:**
- Post-judge fix to `app/setup/actions.ts` (cold-start `ensureSchema()` call) — deliberate scope expansion authorized after the judge flagged a latent first-write race condition. One-line fix; landed as a separate commit (`9c5786e`) in the same PR.
- `app/page.tsx` v0.1 footer text bumped to "v0.2 — supabase + UI shell" — trivially in spirit of the Status update.

**Commit:** `1bcf341e975029e782f97b551f0fd1dc3c2ea478` (squash) — "feat(v0.2): Supabase wiring + auto-migrations + UI shell (#3)"
- Pre-squash commits: `5c8ec95` (main feat) + `9c5786e` (cold-start fix)
**PR:** https://github.com/stone2000ca/tay/pull/3
**Merge verified via `gh pr view`:** MERGED at 2026-05-17T13:36:44Z
**Vercel preview:** not captured (Vercel project not yet linked to GitHub repo; same as run #001)

## Quality check (judge verdict)

═══════════════════════════════════════════
MILESTONE VERDICT

v0.2 (Supabase Marketplace integration + auto-migrations + UI shell with nav):
  Status: APPROVED
  Test evidence: `lib/app-config.test.ts` (8 tests) round-trips both backends incl. single-row invariant via mocked Supabase client; `lib/supabase/migrate.test.ts` (3 tests) exercises skip-path + Promise dedupe + cache-reset; agent reports `npm run build` PASS w/ 5 routes incl. /settings, `npm run typecheck` PASS.
  Tay-specific gates passed: B (PASS — only operational columns in 0001_init.sql), F (PASS — `audit_log.prev_hash`/`this_hash` present as `text`, correct shape for hex SHA256 / human-readable in pgAdmin per requirement), H (PASS — no `dangerouslySetInnerHTML`; `cfg.name` rendered through React auto-escape; no untrusted-content code paths added). C/D/E/I: N/A.
  Rollback verified: weak — agent's report didn't include an explicit rollback procedure; recovery would be `git revert` + manual DROP TABLE. No down-migration file shipped. Acceptable for v0.2 (empty tables), but convention should be set now.
  Notes: Clean, well-commented work; asymmetric error handling between `getFromSupabase` (swallows) and `setInSupabase` (throws) is the one real wart.

═══════════════════════════════════════════
PROCESS SCORE: 4/5

Write-scope respected (zero touches to `builds/`, `PLAN.md`, `skills/`), report structured and honest about live-DB verification gap; missing rollback section and unaddressed cold-start race for the POST path kept it off 5/5. (Cold-start race subsequently fixed.)

═══════════════════════════════════════════
PRODUCT SCORE: 4/5

Code is idiomatic, lazy-initialized, defensively coded (DSN-redaction in error logs, inline-SQL fallback for Turbopack quirks, env-aware cookie `secure`); two small holes — rollback documentation and dual-backend read/write error contract — prevent a 5.

═══════════════════════════════════════════
TOP PROCESS IMPROVEMENTS FOR NEXT RUN

1. **Make rollback explicit in every report.** Judge prompt requires "how would I undo this in 60 seconds." Agent's report omitted it. Orchestrator should require a `Rollback:` field in the structured report — schema work needs a literal down-SQL block; code work needs the revert commit hash.

2. **Asymmetric error contracts deserve a callout.** `getFromSupabase` swallows errors and returns null; `setInSupabase` throws. Defensible (read = soft fail, write = loud fail) but agent never flagged the trade-off in the report or in code comments. Future runs touching dual-backend dispatch should explicitly state the read-vs-write error contract.

═══════════════════════════════════════════
ESCALATIONS REQUIRING USER INPUT

- **Live-DB verification gap.** No real Supabase project linked → migration code never executed against live Postgres. Orchestrator accepted the risk and shipped; first user install is the live smoke test. Mitigations in place. If user wants a pre-customer smoke test, spin up a throwaway Supabase project before v0.3.
- **Cold-start race on `/setup` POST** — fixed in-PR (`9c5786e`). No further action needed.

═══════════════════════════════════════════

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score (judge) | 4/5 |
| Product score (judge) | 4/5 |
| Total token spend (estimate) | not tracked this run |
| Total run time | ~19 min |

## State after this run

**Roadmap progress:** 3/10 milestones MERGED (was 2/10 — added v0.2)

**Next up:**
- v0.3 (Voice calibration — paste 5 emails, extract rubric, save to DB) — eligible. This is the first LLM-touching milestone post-scaffold; v0.3 will NEED a real `ANTHROPIC_API_KEY` in env for the calibration extractor smoke test.

**New blockers:**
- None for v0.3 itself.

**State machine reconciliation:**
- IN_FLIGHT count at end of run: 0 ✅
- INTERRUPTED count: 0 ✅
- PREVIEW_FAILED count: 0 ✅
- BLOCKED count: 0 ✅
- Agent worktree `agent-adf8210112bc0135e` still on disk (branch deleted from remote); safe to reclaim with `git worktree remove --force` since merge is verified.

## Process improvements for next run

1. **Add `Rollback:` to the required agent report structure.** The judge flagged this on run #001 (implicitly) and explicitly on run #002. Bake into the agent prompt template in SKILL.md.
2. **For dual-backend modules, require an explicit read-vs-write error contract comment** in the code itself, not just the agent's report. Saves judge cycles + makes the convention scannable for future maintainers.
3. **Wire `git push origin --delete <branch>` cleanup automatically** when `gh pr merge --delete-branch` aborts on local-checkout. Hit again on this run (same root cause as run #001 — `main` is checked out in parent worktree). Could be a post-merge step in the orchestrator regardless of `gh` exit code.
4. **Pre-stage `ANTHROPIC_API_KEY` for run #003.** v0.3 needs it; this can no longer be deferred.

## Escalations to user

1. **Add `ANTHROPIC_API_KEY` to the orchestrator env before run #003** (v0.3 voice calibration extractor). Add to `.env.local` (see `.env.example`) or accept that v0.3 ships with mocked-only verification.
2. **Optional: live Supabase smoke test.** A 5-minute provisioning step (Supabase Marketplace integration on the live Vercel project) would exercise `migrate.ts`'s real path before the first user install. Not blocking — the inline-SQL fallback + transactional DDL + idempotent CREATEs make this low-risk. Decide before v0.3 starts using DB data.
3. **Vercel project linking.** Still not connected to the GitHub repo, so no preview URLs captured. One-time setup; would also unlock the live Supabase smoke test path above.

## Notes / surprises

- `gh pr merge --delete-branch` failed at local-checkout (same root cause as run #001 — `main` is checked out in the parent worktree `C:/Users/stone/Code/tay`). Server-side merge succeeded; orchestrator manually deleted the remote branch via `git push origin --delete worktree-agent-adf8210112bc0135e`. Worth a SKILL.md tweak — see process improvement #3.
- Build emits a Turbopack NFT-trace warning on `migrate.ts`'s `path.join` for the SQL file. Agent investigated and chose to document inline rather than suppress with `/*turbopackIgnore: true*/` (which didn't silence it). Inline SQL string is the runtime source of truth; disk-load is a fallback that won't trip in production builds. Benign.
- 16-minute end-to-end (preflight → judge approval → fix → merge), one fix-pass. Matches run #001 cadence.

---

*Auto-generated by /tay-build · Run #002 · 2026-05-17*
