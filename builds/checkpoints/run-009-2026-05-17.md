# Run #009 — 2026-05-17

**Duration:** 19:18 → 19:50 UTC (~32 min)
**Milestone:** v0.9 — Reply handler (poll + classify + auto-draft) + v0.8 carry-forwards
**Branch / worktree:** `worktree-agent-ace5a87f0cecf96a8` (deleted post-merge)

## Goal

Replies = fully attacker-controlled. Tay gate H is load-bearing. Build a polling poller (no Pub/Sub infra), an LLM classifier with stacked defenses, an orchestrator that dedupes + routes by intent, and a composed reply drafter (auto-reply OFF by default).

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v0.9 reply handler + v0.8 carry-forwards | `agent-ace5a87f0cecf96a8` | COMPLETE (committed before report this time) |
| Quality judge | (read worktree diffs) | APPROVED |

## What landed

- Migration 0009: replies (UNIQUE gmail_message_id) + gmail_poll_cursor + reply_settings (default OFF) + drafts.reply_to_id
- `lib/oauth/google.ts` extended: scope += `gmail.readonly`; getProfile + getRecentMessages + getMessage; hasReadScope
- `lib/reply/classify.ts` — cheap-model classifier with 6 stacked gate-H defenses
- `lib/reply/poll.ts` — Vercel Cron poller; never throws; first poll seeds cursor (no backfill)
- `lib/reply/handle.ts` — orchestrator with numbered pipeline contract: dedupe → match → classify → branch
- `lib/reply/draft.ts` — composed reply drafter through judge gate
- `/api/cron/poll-gmail` — CRON_SECRET bearer-auth first
- `/replies` page + `/settings` toggle + reconnect banner + Replies nav link
- v0.8 carry-forwards all closed (bad-kind token test; disclosure warn-on-throw; /u/[token] static import + read-before-upsert with new getSuppressionEntry helper)
- 339/339 tests across 30 files (added 30+)

## Tay-specific gates

- B: ✅ schemas operational; classifier prompt forbids demographic inference
- C: ✅ auto-reply drafts pass through withDisclosure(body, { recipientEmail })
- D: ✅ reply drafter uses install-wide voice rubric
- E: ✅ isSuppressed checked before drafting auto-reply (defense in depth on top of unsubscribe branch handling)
- F: ✅ every reply received writes an audit row
- H: ✅ LOAD-BEARING — six stacked defenses; adversarial tests assert structured shape persists
- I: ✅ trust events per branch + override_to_send on auto-reply toggle ON

## Judge verdict

APPROVED. Process 5/5, Product 4/5.

Six non-blocking observations carried to v1.0:
1. (MED) `poll.ts` cursor advances to `getProfile().historyId` not the latest-processed historyId — small race window where messages between history-list and cursor-advance get dropped on next poll
2. (MED) `gmail_poll_cursor` lacks single-row enforcement (random UUID PK + `.update().neq("id","")`); should use deterministic SINGLE_ROW_ID upsert like reply_settings
3. (LOW) Reply body persisted before unmatched-thread skip — "don't store more attacker content than needed" suggests hash/excerpt for unmatched
4. (LOW) Poller burns classifier call on Tay's own outbound messages — cheap fix: pass connected email through and short-circuit
5. (LOW) Composed reply drafter passes empty prospect fields to judge — defensible but type signature suggests more
6. (LOW) Verify settings page renders actual `autoReplyEnabled` value (not just form default)

## Rollback

- Code: `git revert b1e24da7`
- DB:
```sql
DROP TABLE IF EXISTS reply_settings;
DROP TABLE IF EXISTS gmail_poll_cursor;
ALTER TABLE drafts DROP COLUMN IF EXISTS reply_to_id;
DROP INDEX IF EXISTS replies_received_at_idx;
DROP INDEX IF EXISTS replies_thread_id_idx;
DROP TABLE IF EXISTS replies;
```

## Commit / PR / Merge

`3195c79` → squashed as `b1e24da7`. [PR #17](https://github.com/stone2000ca/tay/pull/17).

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score | 5/5 |
| Product score | 4/5 |
| Tests | 339/339 (added 30+) |
| Run time | ~32 min |

## State after

**Roadmap progress:** 10/10 build milestones MERGED. **v1.0 ship gate is next.**

**Next up — v1.0:** JOURNEYS eval suite + trust-tier promotion live + roll up v0.9 carry-forwards. This is the final milestone and the ship gate per PLAN.md.

## Process improvements

(From judge's top 2)
1. Codify the "single-row tables MUST use deterministic SINGLE_ROW_ID + UNIQUE constraint" pattern in the migration template comment so the gmail_poll_cursor drift doesn't recur.
2. Add a "writes BEFORE skip" checklist item: don't store attacker content before deciding whether to keep it.

## Escalations

None blocking. v1.0 should address the two medium-severity polling robustness issues (cursor advance + single-row constraint) since they relate to the polling cadence that JOURNEYS will exercise.

---

*Auto-generated by /tay-build · Run #009 · 2026-05-17*
