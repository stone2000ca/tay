# Run #004 — 2026-05-17

**Duration:** 17:24 → 17:36 UTC (~12 minutes)
**Milestone:** v0.4 (Drafter v1 — prospect → AI-drafted email constrained by voice rubric)
**Branch / worktree:** `worktree-agent-a2a4f08046d641872` (deleted post-merge); merged into `main`
**Trigger:** /tay-build (continuing autonomously per user directive)

## Goal

Single agent. Build the first end-to-end LLM-touching feature: enter a prospect → generate a draft using `MODELS.quality` via OpenRouter, with the voice rubric from v0.3 as the binding constraint. Includes AI-disclosure-footer skeleton (Tay gate C). No send (v0.7), no judge (v0.5), no audit (v0.6).

## Agents spawned

| Task | Subagent type | Worktree | Returned |
|---|---|---|---|
| v0.4 drafter v1 | general-purpose | `agent-a2a4f08046d641872` | COMPLETE |
| Quality judge | general-purpose | (read diffs from agent worktree) | APPROVED |

## Milestone outcome

### v0.4 — Drafter v1

**Plan row:** PLAN.md roadmap, v0.4.
**Status:** MERGED.

**Files added:**
- `lib/supabase/migrations/0003_drafts.sql`
- `lib/draft/disclosure.ts`
- `lib/draft/prompt.ts`, `lib/draft/prompt.test.ts`
- `lib/draft/generate.ts`, `lib/draft/generate.test.ts`
- `lib/draft/persist.ts`, `lib/draft/persist.test.ts`
- `app/draft/page.tsx`, `app/draft/actions.ts`

**Files modified:**
- `lib/supabase/migrate.ts` (`MIGRATIONS_INLINE` +0003; sentinel pre-check refactored to `{kind: "table"|"column"}` for ALTER-bearing migrations)
- `app/page.tsx` (dashboard cascade: app_config → voice rubric → dashboard with `/draft` link + drafts-saved count card)
- `components/nav.tsx` (Draft link added between Dashboard and Setup; still suppressed on `/setup*`)
- `README.md` (status bumped to v0.4)

**Acceptance criteria:**
- ✅ Drafter calls `MODELS.quality` via `getLlmClient`
- ✅ Rubric required: `generateDraft` errors `"Voice not yet calibrated. Complete /setup/voice first."` when no rubric available and no rubric arg passed (tested)
- ✅ All 7 rubric fields render in system prompt as binding constraints (tested per field)
- ✅ `response_format: { type: "json_object" }` set on every call
- ✅ Defensive JSON parse (`stripJsonFences`) + shape validation (subject/body present, ≤200/≤5000 chars)
- ✅ `withDisclosure` injected on every body; idempotent (no double-append; tested)
- ✅ `0003_drafts.sql` idempotent (IF NOT EXISTS on both ALTER and CREATE); `prospect_id` FK with `ON DELETE CASCADE`; `rubric_snapshot` + `prompt_inputs` jsonb preserved for v0.5 re-judge
- ✅ Server action `generateAndSaveDraft` calls `ensureSchema()` first (cold-start guard)
- ✅ Input validation: ASCII-printable on name/company; length caps on all three (notes ≤2000)
- ✅ Dashboard `getDraftCount` soft-fails to null → renders `"—"` when Supabase env absent
- ✅ XSS-safe: subject/body/error rendered via JSX text nodes (auto-escaped); body in `<pre>` for layout

**Tests:**
- `npm run typecheck`: ✅ clean
- `npm run build`: ✅ clean (7 routes — added `/draft`)
- `npm test`: ✅ 49/49 across 7 files (was 29/29; added 20)
- Live OpenRouter call: ❌ DEFERRED (no key in env; user-waived gate)
- Live Supabase migration: ❌ DEFERRED (no project linked)

**Tay-specific gates:**
- B (no special-category data): ✅ — `drafts` schema operational only; `prospects` gains free-text `notes` with explicit SQL-comment warning; system prompt enumerates and forbids special-category inference (tested per category)
- C (AI disclosure footer): ✅ — `withDisclosure` runs on every successful body; idempotent on the marker `"Written with AI assistance"`; system prompt rule 6 instructs the LLM not to add one (avoids race condition)
- D (voice rubric enforcement): ✅ — every rubric field rendered verbatim as constraint; common/avoid phrases rendered as quoted lists; refuses to run without rubric
- H (adversarial-input defenses): ✅ — `<untrusted_source field="...">` wrap + response_format json_object + stripJsonFences + validateDraftShape + server-action ASCII-bound input check + SDK error mapping suppresses raw text (tested)
- E, F, I: N/A

**Write-scope check:**
- All edits match the IN_PROGRESS manifest. No touches to `builds/`, `PLAN.md`, or `skills/`. Verified by judge via `git diff main...HEAD`.

**Migration sentinel correctness (judge probed):**
- Fresh install: all three migrations run in order; 0003's column-sentinel sees freshly-created `prospects` table without `notes` column → runs
- Upgrade from v0.3 (0001+0002 present, 0003 missing): table-sentinels for 0001+0002 return true (skip); column-sentinel for 0003 returns false (runs)
- Steady state: all sentinels return true (all skip)
- Failure cascade: loop short-circuits on any throw; failed 0001 prevents 0003 from running

**Rollback plan:**
```bash
git revert d445d7c0ae3d0881f396027397076882107884e4
# DB (if user already migrated):
DROP TABLE IF EXISTS drafts;
ALTER TABLE prospects DROP COLUMN IF EXISTS notes;
```

**Deviations from scope:**
- `getDraftCount` added to `lib/draft/persist.ts` (READ, soft-fails to null) — needed by dashboard count card
- Sentinel pre-check refactored from `string → table name` to `{kind: "table" | "column"}` discriminated shape (forced by 0003's ALTER pattern)
- `upsertProspect` synthesizes a `unknown+<safeName>@<safeCompany>.invalid` placeholder email because `prospects.email` is NOT NULL (0001 schema decision); documented inline with "v0.5 will add a real email field" note. RFC 2606 `.invalid` TLD guarantees no routing. Edge case noted by judge: company names like `"Acme Inc."` produce labels ending in `-` which violate RFC 1035 — cosmetic only since `.invalid` never routes and column has no format validator. v0.5 cleanup.

**Commit:** `15532964f3cb9c36feba96e2bbcf1d9f20d25502` → squashed as `d445d7c0ae3d0881f396027397076882107884e4`
**PR:** https://github.com/stone2000ca/tay/pull/7
**Merge verified via `gh pr view`:** MERGED
**Vercel preview:** not captured (Vercel project not yet linked)

## Quality check (judge verdict)

═══════════════════════════════════════════
MILESTONE VERDICT

v0.4 (Drafter v1):
  Status: APPROVED
  All four named Tay gates wired with both structural defenses and tests (per-category enumeration in prompt; disclosure idempotency; rubric-required guard; adversarial wrap + response_format + fence stripper + shape validator + length caps + ASCII-bound).
  Migration runner refactor sound across fresh/upgrade/steady-state.
  Dashboard soft-fail honored.
  XSS surface clean.
  Tests: 49/49 pass; typecheck clean; write-scope clean.

PROCESS SCORE: 5/5
Engineering agent followed the read-vs-write contract carry-over cleanly. Deviations minimal, documented, reasonable. Write-scope clean. 49 tests including 20 new ones cover all four gates with explicit assertions.

PRODUCT SCORE: 5/5
The drafter actually works as a brand-safety surface. Defense layers stack and every layer has a test. Dashboard adds real "drafts saved" count card. Form UX clean. The product is shippable and the brand-safety surface holds up to v0.4's threat model.

TOP PROCESS IMPROVEMENTS
1. Orchestrator must create `run-NNN-IN_PROGRESS.md` BEFORE the engineering agent commits — the judge couldn't see the manifest because it was only in the orchestrator's worktree, not in `main`. Either commit/push the IN_PROGRESS file before spawning the agent OR pass the manifest text verbatim in the agent prompt (already doing the latter — judge can fall back to that).
2. Notes-field adversarial test (cheap, high-signal). v0.5 should feed notes containing literal `</untrusted_source>` + embedded instructions to assert structural defenses survive.

ESCALATIONS REQUIRING USER INPUT: None.

═══════════════════════════════════════════

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score (judge) | 5/5 |
| Product score (judge) | 5/5 |
| Total run time | ~12 min |

## State after this run

**Roadmap progress:** 5/10 milestones MERGED (was 4/10 — added v0.4)

**Next up:**
- v0.5 (Judge v1 — 4-way decision over drafts): allow / block / revise / escalate. Reads a draft, the rubric, suppression list (n/a until v0.8), and produces a structured decision. Wires into v0.4's generate flow: every draft passes through the judge BEFORE display. Tay gates C/H definitely apply; F (audit-log writes) becomes relevant as judge decisions are first-class audit events.

**State machine reconciliation:**
- IN_FLIGHT count: 0 ✅
- INTERRUPTED count: 0 ✅
- PREVIEW_FAILED count: 0 ✅
- BLOCKED count: 0 ✅

## Process improvements for next run

1. **Commit and push `run-NNN-IN_PROGRESS.md` to a temporary branch BEFORE spawning the agent**, so the agent worktree (and the judge that reviews it) can read it directly. Avoids the "manifest only in orchestrator worktree" gap the judge flagged.
2. **Carry forward v0.5 paper-cut targets from v0.4's judge:**
   - Synthesize-email edge cases (RFC label hyphen + unicode collision) — fix when real email field lands
   - Notes-field `</untrusted_source>` injection sanitizer + test
   - Pre-flight `hasSupabaseEnv()` check in `generateAndSaveDraft` to avoid wasted LLM calls on misconfigured deploys
   - `/setup/voice` "Supabase not configured" banner (carried from v0.3 judge)
   - Define `SAMPLE_COUNT` once and import (carried from v0.3 judge)

## Escalations to user

None new. Previously-waived gates remain waived per user directive (autonomous mode).

## Notes / surprises

- Total run cycle (preflight → judge → merge → checkpoint) under 15 minutes — fastest run yet. Likely because the v0.3 OpenRouter+voice foundation removed a lot of "what's the right pattern here" overhead.
- Judge gave first 5/5 product score across three runs. The drafter is the first place "all Tay gates apply at once" which forces real defense-layering — when an agent gets that right with explicit per-layer tests, 5/5 is fair.
- Dashboard now has a "drafts saved" count card — first real product surface beyond setup.

---

*Auto-generated by /tay-build · Run #004 · 2026-05-17*
