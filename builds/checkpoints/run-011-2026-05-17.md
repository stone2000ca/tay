# Run #011 — 2026-05-17 — v1.1.1 (first post-1.0 milestone)

**Duration:** 21:34 → 22:20 UTC (~46 min including fix-pass)
**Milestone:** v1.1.1 — secrets foundation + provider-agnostic LLM keys + VERCEL_URL auto-detection
**Branch / worktree:** `worktree-agent-a3e73937ad4ec808b` (deleted post-merge)
**Trigger:** User explicit greenlight after v1.0 ship gate. v3 simplification-plan.md locked after two Sonnet review passes.

## Goal

First milestone of the v1.1 "non-tech user installs in 10 minutes" arc. Lays the foundation by:
1. Moving secrets from user-set env vars into Supabase (collected in wizard or derived via HKDF)
2. Auto-detecting LLM provider from key prefix (sk-ant-/sk-or-/sk-)
3. Auto-detecting site URL from VERCEL_PROJECT_PRODUCTION_URL

After this, the Vercel deploy-time prompt asks for ZERO user-set env vars beyond Supabase Marketplace auto-sets.

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v1.1.1 implementation | `agent-a3e73937ad4ec808b` | COMPLETE (committed before report) |
| Quality judge (first pass) | (read worktree diffs) | NEEDS-FIXES |
| v1.1.1 fix-pass | `agent-a3e73937ad4ec808b` (same worktree) | COMPLETE |
| Quality judge (re-judge) | (read fix diffs) | APPROVED |

## What landed

### Secrets foundation
- Migration 0011: `instance_secrets` single-row table with `lock_col UNIQUE DEFAULT 1`
- `lib/secrets/derive.ts` — HKDF-SHA256 derivation; race-safe bootstrap via `ON CONFLICT (lock_col) DO NOTHING` + re-SELECT; module-scoped Promise cache invalidates on error; env-var fallback for v0.x developers
- `lib/secrets/llm-key.ts` — set/get/getMetadata + SHA-256 fingerprint
- `lib/oauth/crypto.ts` + `lib/unsubscribe/token.ts` refactored to async + per-purpose info strings (cryptographically independent)
- AuditAction extended with `secrets.{salt_bootstrapped, llm_key_set, llm_key_rotated}`

### Provider-agnostic LLM keys
- `lib/llm.ts` rewritten with discriminated-union `getLlmClient()` (no throws on missing key)
- `detectProvider()` from prefix; `chatComplete()` adapter dispatches to OpenAI or Anthropic SDK
- `@anthropic-ai/sdk@^0.39.0` re-added; `getModel(tier, provider)` resolves per-provider
- Cold-start guards on all 5 non-wizard LLM callers (drafter, judge, classifier, reply drafter, voice calibrator)
- Anthropic + json_object: appends "Output ONLY a single valid JSON object" to system message (fix-pass)

### Wizard restructure
- `/setup` is name-only
- New `/setup/llm-key` wizard step with client-side provider hint + server-side re-detect
- `/settings/secrets` page with fingerprint + rotate + Supabase rotation warning
- Redirect chain: app_config → /setup → /setup/llm-key → /setup/voice → dashboard

### VERCEL_URL auto-detection
- `lib/site-url.ts:getSiteUrl()` fall-through chain
- Replaces direct env reads in disclosure + OAuth routes
- Disclosure falls back to constant footer when URL resolves to localhost (no useless dev URLs in real emails)

### Cron route (after fix-pass)
- `process.env.CRON_SECRET` direct read (Vercel auto-sets for cron-enabled projects; HKDF derivation was wrong design — it can't match Vercel's auto-generated value)
- `crypto.timingSafeEqual` with explicit length pre-check
- 503 `cron_secret_not_configured` distinct from 401 `unauthorized`

## Tay-specific gates

- B: ✅ — schemas operational; LLM key + fingerprint columns operational
- C, D, E, I: unchanged
- F: ✅ — every secret rotation audited
- H: ✅ — LLM key never logged; Anthropic SDK adds new untrusted-text path but all existing `<untrusted_source>` wrapping preserved

## Judge process

**First pass:** Process 4/5, Product 4/5 — NEEDS-FIXES. Flagged 6 issues:
1. HIGH: CRON_SECRET design conflicts with Vercel Cron auth (HKDF-derived value can't match Vercel's env-set value)
2. MED: Settings page still flags now-optional env vars as missing
3. MED: HKDF race-safety has no test coverage
4. LOW: Anthropic adapter silently drops `response_format: json_object`
5. LOW: Bearer comparison uses `!==` instead of `crypto.timingSafeEqual`
6. LOW: Empty content defensive insert would fail at Anthropic API

**Fix-pass (`6401861`):** all 7 fixes addressed; cron secret reverted to direct env-var read; race-safety test added via FakeClient mock.

**Re-judge:** APPROVED. Final Process 4/5, Product 4/5.

## Rollback

- Code: `git revert ad39d163`
- DB: `DROP TABLE IF EXISTS instance_secrets;` (additive only)
- Env-var fallback path means v0.x deploys with `TAY_OAUTH_SECRET` in env keep working

## Commits / PR / Merge

- `e74fb29` — feat(v1.1.1): initial implementation
- `6401861` — fix(v1.1.1): judge fix-pass (cron secret + 6 hardenings)
- Squashed as `ad39d163610fb1e6e4be1fabe9754e5c898c9f8c`
- [PR #22](https://github.com/stone2000ca/tay/pull/22)

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score | 4/5 |
| Product score | 4/5 |
| Tests | 421 unit (added 45+) + 10 journeys |
| Run time | ~46 min including fix-pass |
| Fix-pass rounds | 1 |

## State after

**Roadmap progress:** v1.0 complete (11/11 milestones); v1.1.1 merged. v1.x is in progress per simplification-plan.md v3.

**Next up:** v1.1.2 — SMTP send (Easy mode) with interim banner noting "Reply polling activates in v1.1.2.5."

## Process improvements

(From judge's first-pass top 2)
1. Add a "v0.x → vN.x upgrade matrix" review step. Whenever a milestone changes how a user-managed secret or env var is read, the implementation agent must enumerate every shape of existing install and walk the actual request path for each. Cron-secret issue would have surfaced immediately.
2. Require DB-backed integration tests for any file that performs schema bootstrapping or race-sensitive writes. The HKDF math tests prove correctness; they don't prove `bootstrapSalt()` survives concurrent boots. (Addressed in fix-pass for v1.1.1; codify as default for all future v1.x milestones touching schema bootstrap.)

## Escalations

None blocking. Recommended for v1.x:
- The OpenRouter key in `.env.local` (from earlier session) can smoke-test the OpenRouter branch of the new multi-provider `lib/llm.ts`. Anthropic-direct + OpenAI-direct branches stay mock-only until someone has those keys.
- The Anthropic-direct path silently drops `response_format.type === "json_object"` — mitigated by the suffix injection (fix-pass) AND by all current callers having "Output ONLY JSON" in their system prompts. A future caller that doesn't include that instruction would be a regression.

---

*Auto-generated by /tay-build · Run #011 · 2026-05-17*
