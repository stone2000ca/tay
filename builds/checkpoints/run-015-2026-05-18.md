# Run #015 — 2026-05-18 — v1.1.4 (reply notifications + v1.1.3 carry-forwards) — FINAL v1.1 MILESTONE

**Duration:** 00:20 → 00:48 UTC (~28 min)
**Milestone:** v1.1.4 — reply notifications + folded-in v1.1.3 carry-forwards
**Branch / worktree:** `worktree-agent-ae23df6641962acc0` (deleted post-merge)
**PR:** [#28](https://github.com/stone2000ca/tay/pull/28) — squashed as `6953633d`

## Goal

Close the reply loop: when a reply arrives, the user gets notified through their preferred channel (email default; Slack webhook advanced). Also fold in two v1.1.3 carry-forward items (prospect prefill + SSRF allowlist).

**Special significance:** this is the FINAL v1.1 milestone. After this merges, the simplification plan is complete and Tay v1.1 is feature-complete for non-tech-user installation.

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v1.1.4 implementation | `agent-ae23df6641962acc0` | COMPLETE (committed before report) |
| Quality judge | (read worktree diffs) | APPROVED first pass |

## What landed

### Reply notifications
- Migration 0015: `notification_preferences` (single-row; channel `email`/`slack_webhook`/`none`; encrypted slack URL; intent allowlist; optional email override)
- `lib/notify/preferences.ts` — `getPreferences` soft-fails to defaults; `setPreferences` throws; webhook URL validated against `hooks.slack.com/services/` pattern; encrypted via existing crypto layer
- `lib/notify/dispatch.ts` — channel-aware dispatcher; never throws; 5s `AbortController` timeout on Slack POST; CRLF-stripping on email subject/body; webhook URL never logged
- Wire-in: `lib/reply/handle.ts` calls `notifyReply()` after classify + audit + trust as LAST step, wrapped in defense-in-depth try/catch
- `/settings/notifications` UI: channel radio + email override + Slack URL + intent chips + "Send test notification" button

### v1.1.3 carry-forwards (closed)
- **`/draft?prospectId=…` server-side prefill** via new `getProspect(id)` (soft-fails to null). Pre-filled fields editable; no drafter/judge bypass.
- **SSRF allowlist** `isPublicHttpUrl()` on URL fetch. Rejects: loopback, private (10/8, 172.16/12, 192.168/16), link-local, cloud-metadata, CGNAT, IPv4-mapped IPv6, literal `localhost`/`0.0.0.0`. DNS-rebinding caveat documented.

### Audit
- AuditAction extended: `reply.notified`, `notifications.configured`. `from_email_lower` auto-redacted by existing redactor.
- 586 unit tests (added 51) + 10/10 JOURNEYS

## Tay gates

- B: schemas operational only
- C, D, I: N/A for notifications (operator-bound, not prospect-bound). Bypass documented in `dispatch.ts` header.
- E: bypass for self-notifications documented + intentional (user isn't suppressing themselves)
- F: every notification attempt writes `reply.notified` audit (whether sent or skipped)
- H: reply body NEVER in notification payload; `reply.from` sanitized; webhook URL only used as POST target; CRLF-stripped email subject

## Judge verdict

**APPROVED first pass. 5/5 process, 5/5 product.** Third clean-on-first-try v1.1 milestone in a row.

Judge highlights:
- "comprehensive try/catch at every layer"
- "log-probe test proves webhook URL never leaks"
- "DNS-rebinding caveat documented explicitly"
- Read-vs-write contract followed everywhere

One trivial doc/code drift: `sanitizeFromAddress` JSDoc claims U+007F stripped but regex is only U+0000–U+001F. Not load-bearing (CRLF is the actual vector and IS stripped). Tracker note only.

## Rollback

- Code: `git revert 6953633d`
- DB: `DROP TABLE IF EXISTS notification_preferences;`

## Outcomes

| Metric | Value |
|---|---|
| Merged? | yes |
| Process | 5/5 |
| Product | 5/5 |
| Tests | 586/586 unit (+51) + 10 journeys |
| Run time | ~28 min |
| Fix-pass rounds | 0 |

## State after — v1.1 FEATURE-COMPLETE

All 5 v1.1 milestones merged:

| Milestone | Run | Process / Product |
|---|---|---|
| v1.1.1 secrets foundation | #011 | 4/4 (NEEDS-FIXES → fix-pass → APPROVED) |
| v1.1.2 SMTP send (Easy mode) | #012 | 4/4 (NEEDS-FIXES → fix-pass → APPROVED) |
| v1.1.2.5 IMAP reply polling | #013 | 5/5 (clean first pass) |
| v1.1.3 wizard polish | #014 | 5/5 (clean first pass) |
| v1.1.4 reply notifications | #015 | 5/5 (clean first pass) |

**Trend:** 3 consecutive clean first-pass APPROVALs after the v1.1.1/v1.1.2 fix-pass rounds. Foundation work was harder; composition work was smoother.

**Install path is now:** Vercel Deploy → wizard (LLM key → mailbox → voice → rubric preview → sample draft → test-send → first prospect) → done. ~10 minutes, zero terminal commands, zero Google Cloud Console for Easy-mode users.

---

*Auto-generated by /tay-build · Run #015 · 2026-05-18*
*Tay v1.1 simplification arc complete.*
