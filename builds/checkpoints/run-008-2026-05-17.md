# Run #008 — 2026-05-17

**Duration:** 18:51 → 19:14 UTC (~23 min)
**Milestone:** v0.8 — Suppression list + unsubscribe handling + v0.7 carry-forwards
**Branch / worktree:** `worktree-agent-a1e6b130cf309beb6` (deleted post-merge)

## Goal

Tay gate E becomes load-bearing. Real suppression check (safe-default-TRUE on uncertainty). Unsubscribe links in disclosure footer. Resolve all four v0.7 carry-forwards.

## Agents

| Task | Worktree | Returned |
|---|---|---|
| v0.8 suppression + unsubscribe + carry-forwards | `agent-a1e6b130cf309beb6` | COMPLETE (after a follow-up commit-first message) |
| Quality judge | (read worktree diffs) | APPROVED |

Note: the implementation agent finished all work green but didn't commit before declaring done. The orchestrator sent a follow-up via SendMessage to commit (commit SHA `03a55edc`). Pattern to lock in: agent prompt must include "git status MUST be clean before declaring COMPLETE" — it does, but the agent missed it. Process improvement candidate.

## What landed

- Migration `0008_suppression_and_constraints.sql` (suppression table + idempotent ALTER for `sent_messages_draft_id_unique`)
- `lib/suppression/{check,add}.ts` — real impl with safe-default TRUE + idempotent upsert
- `lib/unsubscribe/token.ts` — HMAC-SHA256 + timingSafeEqual + kind-claim + 90-day exp
- `lib/draft/disclosure.ts` — per-recipient unsubscribe link (backward-compat)
- `/u/[token]` — verify → addSuppression → audit; ALL bad states → one generic message; NO recipient email echoed
- `/settings/suppression` — list/add/remove with audit
- v0.7 carry-forwards: UNIQUE constraint + orchestrator 23505 handling; sendDraftAction error redirect; saveGoogleOAuth atomic UPSERT; `subject` in audit redactor
- AuditAction extended with suppression.{added,removed} + user.unsubscribed
- 267/267 tests (added ~50)

## Tay-specific gates

- B: ✅ suppression schema operational only
- E: ✅ NOW LOAD-BEARING — real isSuppressed with safe-default TRUE
- F: ✅ every suppression change audited
- H: ✅ HMAC tokens; no info leak on `/u/[token]`
- C, D, I: unchanged (upstream)

## Read-vs-write contract

- `isSuppressed` — READ but safe-default TRUE on uncertainty (intentional inversion of normal read pattern; documented)
- `addSuppression` / `removeSuppression` — WRITE, throws
- `listSuppressions` — READ, soft-fails to []
- `generateUnsubscribeToken` / `verifyUnsubscribeToken` — pure (throw on missing secret only)

## Judge verdict

APPROVED. Process 5/5, Product 4/5.

Highlights:
- "The load-bearing pieces — suppression check, HMAC token crypto, and the /u/[token] no-leak page — all hold up under probing."
- "The unsubscribe page is a model of restraint — no Tay branding, no email echo, no auth, single generic error message for all failure modes."

Five non-blocking observations for v0.9/backlog:
1. `/u/[token]` replay detection uses a 5-second age heuristic — strictly honest under heavy DB lag would require read-before-upsert
2. Token tests cover most failure modes but NOT bad-kind explicitly
3. `lib/draft/disclosure.ts` swallows `generateUnsubscribeToken` throws silently
4. `/u/[token]` uses dynamic `await import(listSuppressions)` — static import would be cleaner
5. `addSuppression` preserves first-add (correct UX) but `/settings/suppression` UI only shows the first entry — audit log has the full history

## v0.7 carry-forwards CLOSED

- ✅ UNIQUE on sent_messages.draft_id (backstops the orchestrator's race)
- ✅ sendDraftAction error flash (redirect with ?error=)
- ✅ saveGoogleOAuth atomic UPSERT (SINGLE_ROW_ID + onConflict:'id')
- ✅ subject in audit redactor

## Rollback

- Code: `git revert 85f591a9`
- DB:
```sql
ALTER TABLE sent_messages DROP CONSTRAINT IF EXISTS sent_messages_draft_id_unique;
DROP INDEX IF EXISTS suppression_added_at_idx;
DROP TABLE IF EXISTS suppression;
```

## Commit / PR / Merge

`03a55edc` → squashed as `85f591a9`. [PR #15](https://github.com/stone2000ca/tay/pull/15).

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes |
| Process score | 5/5 |
| Product score | 4/5 |
| Tests | 267/267 (added ~50) |
| Run time | ~23 min |

## State after

**Roadmap progress:** 9/10 milestones MERGED.

**Next up:** v0.9 — Reply handler (inbound webhook + threaded LLM).

## Process improvements

1. Agent prompt MUST enforce "git status clean before declaring COMPLETE." Already in the prompt template but missed twice now (runs #001 and #008). Consider hard pre-check: if agent's COMPLETE doesn't include a commit SHA AND clean status, orchestrator auto-replies "commit first."
2. Judge improvement (judge's own #2): for load-bearing crypto modules, require a "negative-path test matrix" in the self-report.

## Escalations

None new.

---

*Auto-generated by /tay-build · Run #008 · 2026-05-17*
