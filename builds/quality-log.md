# Tay Build — Quality Log

Per-run process + product scores from the judge agent. Trend table for spotting drift in build quality over time.

| Run # | Date | Milestone | Status | Process | Product | Notable |
|---|---|---|---|---|---|---|
| 001 | 2026-05-13 | v0.1 setup wizard step 1 | MERGED | 4/5 | 4/5 | Agent didn't commit before declaring done; missing tests on SDK error-mapping seam; cookie `secure: true` will infinite-loop localhost dev |
| 002 | 2026-05-17 | v0.2 Supabase + auto-migrations + UI shell | MERGED | 4/5 | 4/5 | Judge flagged cold-start race on `/setup` POST — fixed in-PR (`9c5786e`). Live-DB verification gap (no Supabase project linked); inline-SQL fallback + transactional DDL mitigate. Cookie `secure` fix from run #001 escalation landed. `next lint` dead-script dropped. |
| 003 | 2026-05-17 | LLM pivot to OpenRouter + v0.3 voice calibration | MERGED | 5/5 | 4/5 | Two coupled milestones shipped in one PR. User pivot: Tay now uses OpenRouter via the OpenAI SDK (`@anthropic-ai/sdk` removed). v0.3 ships voice-calibration extractor with gate H defenses (untrusted_source wrap + response_format json_object + parseRubric hard validator) and gate B (zero special-category fields). Paper cuts: `/setup/voice` wedges if Supabase env absent; SAMPLE_COUNT mismatch between UI (5) and extractor (3-10). |
| 004 | 2026-05-17 | v0.4 drafter v1 | MERGED | 5/5 | 5/5 | First end-to-end LLM feature. All four named Tay gates (B/C/D/H) wired with structural defenses + tests. Migration sentinel refactored to `{kind: "table"|"column"}` for ALTER-bearing migrations. Dashboard `getDraftCount` soft-fails to "—" when Supabase env absent. Non-blocking observations for v0.5: synthesized-email edge cases, notes-field `</untrusted_source>` injection test, pre-flight Supabase env check. First 5/5 product score — drafter is where defense-layering pays off. |
| 005 | 2026-05-17 | v0.5 judge v1 + audit stub + paper cuts | MERGED | 5/5 | 4/5 | Load-bearing milestone. Judge wired with all 4 Tay gates as VERIFICATION (not trust). `parseJudgeDecision` hard validator. Audit-log stub (Tay gate F infrastructure; v0.6 wires hash chain). 4 of 5 paper cuts from v0.3/v0.4 cleared (synthesized-email punted, cosmetic). 96/96 tests. v0.6 flags: appendAudit redactor doc-vs-impl drift; neuter() covers closing tag only; sanitizeReasons cap-vs-reject. |
| 006 | 2026-05-17 | v0.6 audit log v1 — sha256 hash chain | MERGED | 5/5 | 4/5 | Tay gate F NOW FULLY LIVE. `lib/audit/hash.ts` pure (canonical JSON + NULL sentinel + sha256). `appendAudit` rewritten (public API preserved); never throws; redacts before hashing. `/api/audit/verify` + `/audit` page. All 3 v0.5 carry-forwards cleared. 143/143 tests. v0.7+ polishes: hash domain separator (currently safe-by-coincidence); AuditVerifyResult shape (unavailable/read_error nested under brokenAt rather than top-level variant). |
| 007 | 2026-05-17 | v0.7 Gmail OAuth + send path | MERGED | 5/5 | 4/5 | First emission to a third party. All 7 Tay gates active. AES-256-GCM encryption at rest. Raw-fetch Google OAuth + Gmail. Orchestrator chokepoint with explicit ordering test (isSuppressed → ensureFreshAccessToken → sendEmail). Log-probe test asserts recipient/subject/body/token never appear in error logs. `appendAudit` extended with oauth.{connected,disconnected} + send.sent. 216/216 tests. v0.8 flags: UNIQUE on sent_messages.draft_id (race under concurrent clicks); sendDraftAction silent-failure flash. Judge: "the most carefully-engineered PR in the build." |
| 008 | 2026-05-17 | v0.8 suppression list + unsubscribe + v0.7 carry-forwards | MERGED | 5/5 | 4/5 | Tay gate E now LOAD-BEARING. Real `isSuppressed` (safe-default TRUE on uncertainty). HMAC-SHA256 unsubscribe tokens (timingSafeEqual + kind claim + 90-day exp). `/u/[token]` collapses all bad states to one generic message — NO recipient email echoed (no enumeration / phishing surface). All 4 v0.7 carry-forwards CLOSED (UNIQUE on sent_messages.draft_id; sendDraftAction redirect; saveGoogleOAuth UPSERT; subject in redactor). 267/267 tests. v0.9 observations: replay 5s heuristic; bad-kind test missing; disclosure swallows token throws. Judge: "the unsubscribe page is a model of restraint." Agent forgot to commit before declaring done (twice now — runs #001 and #008); process improvement to enforce. |
| 009 | 2026-05-17 | v0.9 reply handler + v0.8 carry-forwards | MERGED | 5/5 | 4/5 | Tay gate H most load-bearing yet (reply body fully attacker-controlled). Six stacked defenses: untrusted_source wrap + system prompt naming attack phrases + response_format json_object + parseReplyClassification hard validator + quote-strip + neuter on emitted reasons. Polling poll.ts never throws (returns counts). Auto-reply OFF by default. All 3 v0.8 carry-forwards closed (bad-kind token test; disclosure warn-on-throw; /u/[token] read-before-upsert). 339/339 tests. v1.0 carry-forwards (6 non-blocking, 2 medium): cursor advance race; gmail_poll_cursor single-row enforcement; reply body persisted before unmatched skip; classifier burns on own outbound; reply drafter passes empty prospect fields; settings page render honesty. Agent committed before declaring done this run. |
| 010 | 2026-05-17 | **v1.0 SHIP GATE** — JOURNEYS + trust-tier + v0.9 polling fixes | MERGED | 3→5/5 | 4/5 | First fix-pass round in v0.x. Judge first-pass NEEDS-FIXES (2 JOURNEYS scenarios were mock-trusting tautologies — audit-chain didn't call verifyAuditChain; suppression didn't call sendDraft). Fix-pass commit rewrote both to call real production code. Re-judge APPROVED. 10/10 JOURNEYS scenarios cover all 7 Tay gates; trust-tier ladder tier_0→tier_1→tier_2 (auto) with demotion logic; all 6 v0.9 polling carry-forwards closed. 370 unit + 10 journey tests green. **Tay v0.x BUILD CYCLE COMPLETE.** |

---

## Scoring notes

- **Process score (1-5):** how well the agent + orchestrator worked. 5 = clean run, scope respected, judge had nothing to flag. 1 = chaos / fixes / re-runs.
- **Product score (1-5):** quality of the merged work itself. 5 = production-grade with comprehensive tests. 1 = shipped-but-fragile.
- **Honest rubric.** A 5/5 product score should be rare. A 4/5 product score is normal for shipping.
- **Trend warnings:** if process score < 3 for two consecutive runs OR product score < 3 for a single run, surface to user before next run kicks off.
