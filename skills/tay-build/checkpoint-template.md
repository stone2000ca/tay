# Checkpoint template

Per-run report. Written to `builds/checkpoints/run-NNN-YYYY-MM-DD.md`.

---

# Run #NNN — YYYY-MM-DD

**Duration:** HH:MM (start) — HH:MM (end), N minutes
**Milestone:** v<VERSION> (<description>)
**Branch / worktree:** <branch name>
**Trigger:** /tay-build [args]

## Goal

<the run plan from Phase 2 of /tay-build, verbatim>

## Agents spawned

| Task | Subagent type | Worktree | Returned |
|---|---|---|---|
| v<VERSION> — <description> | general-purpose | <worktree path> | COMPLETE / NEEDS-INPUT / BLOCKED |
| Quality judge | general-purpose | (reads diffs via `git diff`) | APPROVED / NEEDS-FIXES |

## Milestone outcome

### v<VERSION> — <description>

**Plan row:** PLAN.md roadmap, v<VERSION>

**Status:** MERGED | PARTIAL | INTERRUPTED | BLOCKED | PREVIEW_FAILED

**Files added:**
- <path>

**Files modified:**
- <path>

**Acceptance criteria:**
- ✅ <criterion 1> — <evidence>
- ✅ <criterion 2> — <evidence>
- ❌ <criterion 3> — <reason>; deferred to follow-up

**Tests:**
- `npm run typecheck`: ✅
- `npm run build`: ✅
- `npm run lint`: ✅ / N/A
- Smoke test (if LLM-touching): ✅ via $env:ANTHROPIC_API_KEY
- Schema migration up+down (if applicable): ✅
- (or NEEDS-INPUT entries with details)

**Tay-specific gates:**
- B (No special-category data): ✅ / N/A
- C (AI disclosure footer): ✅ / N/A
- D (Voice rubric enforcement): ✅ / N/A
- E (Suppression on send paths): ✅ / N/A
- F (Audit log writes on Tier-3): ✅ / N/A
- H (Adversarial-input defenses): ✅ / N/A
- I (Trust-tier write paths): ✅ / N/A

**Write-scope check:**
- Files actually edited: <list>
- Out-of-scope edits: <none | list with judge's flag>

**Rollback plan:**
<the agent's rollback plan — file revert list, down migration SQL if schema change>

**Deviations from scope:**
- <none | description with reason>

**Commit:** <SHA> "<commit message>"
**PR:** <URL>
**Merge verified via `gh pr view`:** <state from GitHub API — MERGED / OPEN>
**Vercel preview:** <URL if captured>

## Quality check (judge verdict)

(Paste the judge agent's structured verdict verbatim — see judge-prompt.md output format)

## Outcomes

| Metric | Value |
|---|---|
| Milestone merged? | yes / no |
| Process score (judge) | N/5 |
| Product score (judge) | N/5 |
| Total token spend (estimate) | $X.XX |
| Total run time | HH:MM |

## State after this run

**Roadmap progress:** X/10 milestones MERGED (was X-1/10 before this run)

**Next up:**
- v<NEXT> (<description>) — eligible

**New blockers:**
- <none | description>

**State machine reconciliation:**
- IN_FLIGHT count at end of run: 0 (cleared as expected)
- INTERRUPTED count: <0 normally>
- PREVIEW_FAILED count: <0 normally>
- BLOCKED count: <0 normally> with worktree preserved at <path>

## Process improvements for next run

(From judge's "Top improvements" + your own observation)

1. <specific actionable improvement>
2. <specific actionable improvement>

## Escalations to user

(Anything requiring human decision before next run)

- <escalation> — <what user needs to decide>

## Notes / surprises

(Free-form)

---

*Auto-generated by /tay-build · Run #NNN · YYYY-MM-DD HH:MM*
